# Open Historia — 로컬 Pax Historia 프로젝트 계획서

**최신화: 2026-08-02 · 18라운드 종료 시점 (게임 내 2017-05-25, 문재인 정부 출범)**

---

## 0. 이 프로젝트가 무엇을 지키려 하는가

기능 목록보다 먼저 적어둘 원칙이 있습니다. 지금까지 내린 판단의 절반은 여기서 나왔습니다.

**쌓아둔 행동은 전부 이행되거나, 눈에 보이게 남는다.** 원본에서 장기전을 돌릴 때 토큰을 아끼려고 행동을 한꺼번에 집어넣으면 뒷부분이 통째로 갈려나갔습니다. 이 프로젝트는 그걸 고치려고 시작했습니다. 그래서 "조용히 사라짐"은 이 엔진에서 버그로 취급합니다 — 처리되지 못한 명령은 큐에 남아 다음 턴에 다시 보여야 하고, 처리됐다는 표시는 실제로 그 일이 서술됐을 때만 붙습니다.

**모델이 틀릴 수 있는 자리는 엔진이 막는다.** 12B 로컬 모델은 프롬프트를 아무리 강하게 써도 특정 실수를 반복합니다. 평판 칸에 변화량을 쓰고, 마커 종류를 catch-all로 뭉개고, id를 산문에 적고, 자기 대화 기록에서 잘못된 호칭을 배웁니다. 프롬프트로 다섯 번 시도해서 안 되면 그건 프롬프트 문제가 아니라 검증 부재 문제입니다.

**서사와 상태는 절대 어긋나지 않는다.** 이벤트가 영토를 뺏었다고 쓰면 지도가 움직여야 하고, 지도자가 바뀌었다고 쓰면 통계 시트가 따라야 합니다. 반대도 마찬가지입니다 — 시트가 바뀌는데 서사가 없는 것도 같은 버그입니다(18라운드의 대선이 그랬습니다).

**플레이어는 국가원수가 아니라 플레이어다.** 국가 시트의 지도자는 별개의 인물이며 교체·실각·사망할 수 있습니다. 플레이어는 계속됩니다. 이 캠페인에서 실제로 박근혜 → 황교안 권한대행 → 문재인으로 세 번 바뀌는 동안 플레이어는 그대로였습니다.

**측정으로 가를 수 없으면 임계값을 세우지 않는다.** 문자 유사도로 "재탕 대 진짜 후속 사건", "이행된 명령 대 방치된 명령", "중복 이벤트 대 같은 날의 정당한 두 사건"을 갈라보려고 세 번 시도했고, 세 번 다 실측 점수 구간이 겹쳤습니다(0.400 대 0.750, 0.169 대 0.174, 0.500 대 0.421). 겹치는 자리에 임계값을 세우면 진짜를 죽입니다. 그런 문제는 원인 쪽에서 고치거나(정보를 안 보여줘서 반복한 것), 더 나은 신호가 생길 때까지 보류합니다(E의 관계 수치).

**하나 때문에 전부 잃지 않는다.** 조용히 사라지는 건 없어야 하지만, 위도 하나가 95라고 완성된 턴 전체를 폴백으로 버리는 것도 손실입니다. 고칠 수 있으면 고치고(범위 클램프), 못 고치면 그것만 버리고 소리 내서 알립니다(op 단위 드롭). 전부 지키는 것과 하나 때문에 전부 잃는 것은 다릅니다.

---

## 1. 지금까지 완료된 것

### 지도

도시와 구조물 글리프가 통째로 사라지던 문제를 해결했습니다. 원인은 글리프와 이름표가 같은 소스에 있어 서로를 밀어냈던 것이고, `crossSourceCollisions={false}` 아래에서 소스를 나눠 각자 자기들끼리만 솎아내게 했습니다. 중국 도시 밀집은 인구 기준이 아니라 화면 공간 기준으로 솎습니다 — 전 세계 300만 이상 도시 389개 중 244개가 중국인 건 인구 수치가 행정구역 단위여서 생기는 문제라 인구 임계값으로는 풀리지 않습니다.

프로빈스 경계는 멀리서 보이지 않고 가까이서만 희미하게 보입니다. 해역은 상시 표시로 바꿨고 영해 장악력 표현의 토대가 됩니다. `regions-fill`의 `fill-opacity`가 잘못된 표현식이라 맵 에디터로 프로빈스를 하나라도 수정한 시나리오는 z6.5 이상에서 정치 색이 통째로 사라졌던 것도 고쳤습니다. 구조물은 이름표를 클릭해도 선택되고, 새 마커는 기존 마커·도시와 겹치지 않게 배치됩니다.

### 행동 큐

큐 해결은 네 겹입니다. 모델이 직접 적은 `actionIds`, 산문에 박아넣은 별칭 회수(마커 이름·설명까지 포함), 문자 바이그램 매칭, 그리고 전용 매핑 호출(`actionCoverage`). 여기까지가 커버리지를 **더하는** 층이고, 마지막에 **빼는** 층이 있습니다: 모든 주장을 그 이벤트 본문과 대조해서 그 명령에 관한 이벤트가 아니면 주장을 취소하고 큐에 되돌립니다. 임계값은 실측으로 잡았습니다 — 허위 주장 0.03~0.04, 정상 주장 0.60~1.00, 바는 0.25.

명령을 처리한 이벤트는 정의상 플레이어 관련이므로, `actionIds`를 실은 이벤트가 세계 이벤트 행세를 하면 강제로 재라벨합니다. 쿼터를 세기 전에 한 번, 커버리지가 확정된 뒤에 한 번 — 유료 매핑과 세계 패스가 라벨 이후에도 id를 붙이기 때문에 두 번이 필요합니다(실측: 명령 5건을 실은 "세계" 이벤트가 세계 패스에서 나왔습니다).

턴 저장이 진행 중 추가된 명령을 덮어쓰던 문제, 액션 id 중복, 저장 파일 비원자적 쓰기(중간에 끊기면 큐 전체 소실)도 막았습니다.

### 연표 — A·B 완료

계획의 A(기간별 연표 주입)와 B(분기 이후의 계약)가 구현되어 실측 검증까지 끝났습니다. 원래의 문제의식: 기본 시나리오의 시대 정보가 0자라 모델이 지도와 날짜만 쥐고 있었고, 그 결과 2016년 캠페인에서 4차 핵실험도 사드도 총선도 나오지 않았으며 빈자리를 "러시아 극동 개발" 재탕으로 채웠습니다.

**구조.** 시나리오가 자기 일정을 들고 옵니다(`data/timelines/modern-2016.json` — 2016~2017년 42개 항목, pivotal 19·effect 11·foreseeable 11). 점프마다 그 구간에 걸치는 항목을 프롬프트에 싣고, 턴이 끝나면 전부 대조해 답해지지 않은 것을 백로그로 다음 턴에 넘깁니다. 엔진은 그게 진짜 역사인지 작가가 지어낸 역사인지 알 필요가 없습니다 — 외부 프롬프트 시나리오든 완전 가상 세계든 같은 기계가 돕니다.

**분기 계약.** 예정된 사건은 기본적으로 일어납니다. 달라질 수 있는 경우는 캠페인 안에서 실제로 그렇게 만든 것이 있을 때뿐이고, 그때는 **그 분기 자체가 이벤트로 서술되어야** 합니다. "일어나거나, 막힌 서사가 쓰이거나, 미뤄진 이유가 쓰이거나 — 조용한 부재는 실패다"가 프롬프트 계약에 박혀 있습니다. 반대 방향의 보호도 같이: 플레이어가 역사를 알고 선제 대응하는 명령은 정당한 플레이이며, "그 시점의 국가는 알 수 없었다"는 이유로 약화·재해석되지 않습니다.

**예지 게이트.** 브레인스토밍(제안 보드)에는 공개적으로 예정된 것만 넘어갑니다 — 선거일은 달력에 있지만 결과는 비밀이고, 핵실험은 아예 안 보입니다. `foreseeableFrom`으로 "브렉시트 투표일은 2월에야 확정됐다" 같은 중도 공개도 표현합니다. 플레이어가 직접 타이핑한 명령은 이 필터와 무관합니다.

**effect — 서술이 상태를 못 움직일 때.** 연표 항목은 `effect`로 세계 상태 변화를 직접 선언할 수 있습니다. 모델이 서술만 하고 impacts를 비워 보내면 엔진이 대신 씁니다 — 호세프 파면을 서술하고 브라질 지도자는 안 바꾼 실측 사례에서 나왔습니다. 모델이 스스로 채우면 덮어쓰지 않습니다(플레이어가 지도자를 먼저 갈아치운 대체역사가 우선). 이 경로로 테메르·두테르테·메이·트럼프·황교안 권한대행·문재인·마크롱·음낭가과가 실제로 시트에 들어갔고, 풍계리 핵실험장이 지도에 올라갔습니다.

**리비전 갱신.** 배포 연표는 세이브에 복사본으로 들어가되 출처와 리비전이 찍힙니다(`periodTimelineSource`). 파일을 고치고 `revision`을 올리면 진행 중인 캠페인도 다음 점프에서 자동 갱신됩니다 — 실측: 손으로 주입했던 18개짜리 세이브가 리비전 2(20개), 리비전 3(42개)로 두 번 자동 이행됐습니다. 범위 밖 캠페인이나 작가가 직접 쓴 일정은 건드리지 않습니다.

**대조는 제목+본문 전체로만 합니다.** 제목 단독 점수와의 max를 쓰던 초기 구현은 "제19대 대통령 선거"를 "정부 행정 효율화…" 이벤트에 **대통령** 한 단어(0.333)로 배정해버렸고, 그 결과 대선이 화면에 한 번도 안 나온 채 지도자만 바뀌었습니다. 전체로 재면 그 쌍은 0.083/0.059로 떨어지고 진짜 매치(김정남 0.571, 파면 0.429, 시리아 0.316, 호세프 0.769)는 전부 남습니다.

**날짜 준수.** "항목의 날짜에 써라"가 계약에 있고 14일 이상 밀리면 로그에 남습니다. 실측 추이: 도입 직후 18일 밀림(9/9 → 9/27) → 이후 김정남 2/13, 사드 3/6, 헌재 파면 3/10 전부 정확, 시리아 타격만 2일 오차.

### 세계 이벤트 반복(루프) 억제

여섯 턴 내내 "러시아 극동 개발" ×6, "중국 디지털 실크로드" ×7, "사우디 비석유 산업" ×6이 재탕되던 문제. 원인이 셋이었고 셋 다 다른 종류였습니다.

**필터가 반대쪽 끝을 보고 있었습니다.** 이벤트 저장소는 오래된 것부터인데 반복 필터 창이 `.slice(0, 24)` / `.slice(0, 60)` — 캠페인이 커진 뒤로는 12월 이벤트를 1월 것과 비교하고 있었습니다. `slice(-24)`/`slice(-60)`으로 고치고 전 캠페인 재생으로 검증: 13 → 15건 적발, 유실 0, 진짜 사건(쿠데타·올림픽·탄핵·대선·동알레포)은 전부 생존.

**모델이 자기가 뭘 말했는지 못 봤습니다.** 일반 최근 이벤트 창은 플레이어 이벤트에 밀려 세계 헤드라인이 남지 않습니다. `[Already Reported — Do Not Retell]` 블록이 최근 세계 헤드라인 20개를 점프와 세계 패스에 직접 보여주고, "한 번 발표한 계획은 발표된 것이다. 다음에 보도할 가치가 있는 건 결과다 — 결과에는 impacts가 실린다. 새 소식 없는 나라면 다른 나라를 써라"를 지시합니다. 12개로는 부족했습니다 — 러시아 극동의 직전 판본이 창 바로 바깥에 있어 반복됐고, 20개로 넓혔습니다.

**유사도로 더 조이는 건 포기했습니다(원칙 5).** 이름을 바꾼 루프(0.161~0.321)와 진짜 서로 다른 사건(0.043~0.174)이 겹치고, "중동의 수소 경제 통합 가속"은 재탕이 아니라 프롬프트가 요구한 바로 그 '결과'(협정 체결)일 수 있습니다. 근본 원인은 연표가 2016-12-22에서 끝나 2017년이 다시 무연표가 된 것이었고, 2017년 22개 항목을 채우자 루프가 실제로 끊겼습니다.

**세계 패스의 정직성.** 세계 이벤트 쿼터가 부족할 때만 도는 세계 패스는 연표를 받지 않습니다 — 받게 했더니 본 생성이 쓴 예정 항목을 한 번 더 썼습니다(헌재 파면이 한 턴에 두 번). 연표는 본 생성이 책임지고, 놓친 건 백로그가 다음 턴으로 나릅니다.

### 세계 상태 무결성

0~100 standing(안정성, 주권, 식량·에너지 자율, 경제 독립, 내부 보안, 국제 평판)은 이벤트가 설명하지 못하는 급변을 거부합니다. 모델이 절대값 칸에 변화량을 쓰는 실수 — 평판 57에서 "1", 안정성 95에서 "5" — 를 잡습니다.

통계 시트의 경제 블록은 값이 양처럼 읽히는지 검사합니다. 복구 잔해(`)}}'*, {`)가 GDP로 표시되던 것을 막고, 이미 저장된 시트에도 같은 검사를 겁니다. 시트 캐시 키가 런타임 토큰을 물고 있어 재접속마다 재생성되던 것도 고쳤습니다. 시트는 캠페인 날짜 기준으로 생성됩니다 — 2016년 1월 한국 대통령이 문재인으로 나오던 문제.

이벤트 제목·마커 이름·마커 설명의 복구 잔해와 내부 id도 잘라냅니다(id에 붙어 있던 조사가 고아로 남는 것까지 — 단, 실제로 뭔가 제거됐을 때만 작동해서 "이 시설은…" 같은 정상 문장은 안 건드립니다).

### 스키마 관용 — 턴을 잃지 않는 파싱

`runJsonTask`의 방어층: 재시도, 패치-머지(재시도가 지적된 조각만 보내면 이전 최선 후보에 얹음), 관대한 JSON 파싱, 스키마 가지치기(모르는 필드는 로그 남기고 제거), 문자열↔숫자 무손실 상호 변환, 유도 가능한 필수 필드 복원(summary·stopDate), 잘린 JSON salvage.

여기에 두 층을 추가했습니다. **범위 밖 숫자는 클램프합니다** — 위도 95 하나가 anyOf 전 분기를 탈락시켜 완성된 턴 전체(이벤트 전부, 명령 19건, 연표 항목 1건)가 폴백으로 떨어진 실사고에서 나왔습니다. 95를 쓴 모델은 "북쪽 끝"을 뜻했고 좌표는 어차피 틀렸으니, 자르고 나머지 서른 가지를 살리는 쪽이 맞습니다. **그래도 안 맞는 op는 그 op만 버립니다** — 필수 필드가 아예 없으면 클램프로 못 고치므로, op 하나를 소리 내어 버리고 이벤트와 턴은 살립니다. 같은 모양의 사고가 네 번째였고, 필드를 하나씩 화이트리스트에 넣는 방식은 지는 싸움이었습니다.

### 배치 검증 — 시설은 소유국 땅에 선다

실측 42개 중 6개가 국경 밖이었습니다 — 대한민국 지휘소 두 곳이 휴전선 이북, 중국 데이터 센터가 키르기스스탄, 세 곳이 바다. 시나리오가 이미 답을 갖고 있어서(regionsGeojson = admin-1 전 세계 폴리곤 + owner, 정복은 override로 반영) 엔진이 물어봅니다(`runtime/territory.js`).

**기하는 단순화하지 않습니다.** 2km 솎기가 메모리를 6.8MB로 줄여줬지만, 두 지휘소의 보정 목표 지점이 2km 해상도에선 경기도/대한민국, 전정밀도에선 강원도/북한이었습니다. 국경 근처에서 단순화는 근사가 아니라 오답이고, 그 근처가 바로 이 오류가 나는 자리입니다. 전정밀도 19MB·조회 0.6ms로 갑니다.

**옮기지 않는 것이 더 중요합니다.** 기준은 허가가 아니라 거리 — 국경에서 1.5° 이내의 실수만 되돌리고, 대사관과 의도적 해외 시설은 그대로 둡니다. 안쪽으로 밀어도 소유국 땅에 못 넣으면 옮기지 않고 로그만 남깁니다. 해외 배치는 이제 "누구 소유가 어느 나라에 서 있다"로 매 턴 보고됩니다(침묵이 미국 사드를 개성에 세워둔 채 통과시킨 뒤에 넣었습니다).

**충돌 회피가 국경을 넘지 못하게 했습니다.** 겹침 방지 나선의 반경이 최대 150km인데 자기가 무슨 땅 위를 걷는지 몰라서, 배치 검증이 고쳐놓은 시설을 도로 북한에 옮긴 회귀가 있었습니다. 소유국 땅에 선 시설은 나선이 국경 밖 후보를 거부하고, 집 안에 빈자리가 없으면 겹침을 감수합니다 — 겹침은 눈에 보이지만 나라가 틀린 건 안 보입니다.

**주인 없는 시설은 밟고 선 땅의 주인에게 귀속합니다.** 추측이 아니라 사실이고, 그래야 검증이 적용됩니다.

### 구조물 관리 — 지도는 장소지 사업 목록이 아니다

이름이 프로그램인 마커("…시스템", "…전략", "…계획")는 지도에 올리지 않습니다 — 그 사업을 만든 이벤트는 그대로 남습니다. 언더스코어 이름(`정밀_타격분석센터`)은 라벨답게 풀어 씁니다. 종류는 이름에서 주제→형태 순으로 추론해 catch-all을 줄였습니다(landmark 8/11 → 이후 대부분 구체 종류).

**무분별 증식의 원인 셋을 18라운드에 잡았습니다.** 실측: 80개 중 수소·에너지 16, 양자·반도체 14. (1) 프롬프트의 구조물 목록이 `slice(0, 60)` — 오래된 60개라 **가장 최근 20개가 안 보였고**, 지난달에 지은 걸 못 보니 또 지었습니다. `slice(-60)`으로 고쳤습니다. (2) 60줄 평면 목록은 "이미 14개 있음"으로 안 읽힙니다 — 종류별 집계(`power plant ×11…`)와 "새로 짓기 전에 이 목록을 읽어라. 목적이 같으면 짓지 말고 확장하라"를 앞에 붙였습니다. (3) 같은 턴에 지은 것끼리는 비교조차 안 했습니다 — 병합 검사가 턴 시작 시점의 지도만 봐서 `…보안 센터`와 `…보안 구역`이 나란히 생겼습니다. 이번 턴 생존자도 다음 것과 비교합니다.

병합 판정은 **이름으로만** 합니다. "같은 소유·같은 종류·25km" 규칙은 "둘 다 서울에 있음"으로 환원되어 무관한 시설 둘을 지운 전과가 있습니다. 거리는 정체성이 아니고, 이름이 정체성입니다.

기존 80개는 테마+지리 근접(0.6°)으로 통폐합해 **44개**로 줄였고, 흡수된 시설명은 남은 시설의 설명에 "통합 시설: …"로 보존했습니다. 성격상 유일한 것(풍계리, 권한대행 집무실, 사드, 쿠알라룸푸르)과 해외 거점은 건드리지 않았습니다. 턴당 건축 상한(8)은 그대로 뒀습니다 — 평균 5.1개, 상한 도달 17턴 중 2턴이라 문제는 양이 아니라 중복이었습니다.

### 언어 · 지명 현지화

번역 캐시의 하이브리드 오염(`Chagang-do → 차anggan도`)을 읽기 시점에도 검사합니다 — 쓰기 가드만 있어 이미 저장된 오염이 영원히 재적용되던 문제로, 실기기 80건·서버 팩 4건을 정리했습니다.

모델에게 지리 이름은 전부 영어로 들어가므로(엔진이 영어로 매칭) 한국어 문장 한가운데에 영어로 되돌려 씁니다 — "Russia는 시베리아를…", "(P'yŏng양 등)". 엔진은 이걸 전부 한국어로 알고 있어서(지도에 그리니까) 번역이 아니라 **조회**로 풉니다: 국가·행정구역 목록으로 사전을 만들어 이벤트·마커·제안 보드·턴 마커까지 치환합니다. 행정 접미사 생략형(Gyeonggi ← Gyeonggi-do)도 충돌이 없을 때만 별칭으로 잡고, `P'yŏng양` 같은 반쪽 번역은 라틴 앞부분과 한글 꼬리가 같은 이름을 가리킬 때만 복원합니다. 사전 밖은 절대 안 건드립니다 — AI·SMR·DMZ·K-Shield·'Vision 2030'은 한국어 화자가 그렇게 쓰고, Chinatown이 China로 바뀌지 않고, Guinea-Bissau가 Guinea를 가로채지 않습니다.

엔진이 직접 쓰는 문구(턴 마커의 잔여 명령 안내)도 언어팩을 태웁니다.

### 플레이어 정체성 · 호칭

어드바이저와 외교문에는 "플레이어는 국가원수가 아니다" 지시문이 붙습니다 — 지도자는 별개 인물로 존재하고, 연설·서명·정상회담은 지도자가 이름으로 합니다. 그런데 어드바이저가 답변 11개 연속 "대통령님,"으로 시작한 실측이 있었고, 원인은 지시문이 아니라 **대화 기록 재생**이었습니다: 자기 답변 열한 개를 방금 읽고 새 답변을 쓰니 영어 지시문 한 줄이 실전 예시 열한 개를 못 이깁니다. 틀린 습관이 스스로를 가르치는 구조라 엔진이 끊습니다(`runtime/playerAddress.js`) — 답변에서, 스트리밍 중에, 그리고 **재생되기 전의 기록에서** 호격을 제거합니다.

지우는 건 호격뿐입니다. "박근혜 대통령이 파면되었습니다"처럼 지도자를 **언급**하는 건 픽션이 작동하는 것이므로 전부 살아남고, 이름이 앞에 붙은 직함은 서술로 보고 안 건드립니다. 외교 채팅에도 같은 규칙이 걸립니다(타국이 플레이어를 동급 국가원수로 부르는 것). 지시문에는 실제로 나오는 형태를 그대로 나열했습니다 — 추상적 "any equivalent"보다 12B에게 훨씬 셉니다.

### 국가 성격

다섯 축(공격성·위험감수·충성도·팽창주의·복수심)이 국가마다 지속됩니다. 저장 프로필이 없으면 태그와 standing에서 결정론적으로 파생하므로 태그 없는 세계에서도 국가마다 다릅니다. 표시는 난이도로 개방 — 어려움까지 공개, 매우 어려움 이상 숨김, 자국도 동일. 숨기는 건 표시뿐이고 시뮬레이션에는 항상 실립니다.

### 프롬프트 예산

26,917 → 약 9,500 토큰. 섹션별 우선순위를 두고 예산 안에서 자릅니다(우선순위 9는 절대 안 잘림). 한국어는 영어와 토큰 밀도가 달라(≈1.6자/토큰) 문자 기준 예산이 2배 넘게 과소평가한다는 걸 반영했습니다. 18라운드 기준 총 13~14.5k/20,960으로 여유는 있지만 campaign 섹션이 3,638 → 4,926으로 커지는 추세입니다 — C(원장)의 착수 신호로 봅니다.

---

## 2. 다음 작업 — 우선순위 순

### C. Summarizer / Key-Value 상태 원장

턴이 진행될수록 프롬프트가 무거워지는 문제의 근본 대책이자, 지금 남은 것 중 가장 큽니다. 매 턴 끝에 별도의 작은 호출이 주요 사건·조약·영토 변화를 Key-Value로 압축해 누적 저장하고, 프롬프트는 전문 대신 원장을 싣습니다. 지금은 이벤트 히스토리를 잘라 넣고 있어 오래된 맥락이 그냥 사라집니다. campaign 섹션 증가 추세(위)가 착수 근거입니다.

### D. 이벤트 생성 워크플로우 — 남은 단계

7단계 원안: 플레이어 행동 반영 → 외교 결과 반영 → 국제사건 초안(주체별 가중치) → 상세화 → 반응 이벤트 → 맵 변경 검증 → 채팅 발신. 이 중 상당수가 다른 형태로 흡수됐습니다 — 6(맵 변경 검증)은 좌표·소유·범위의 결정론 검증으로, 3의 중복 방지는 반복 필터 + Already-Reported 목록으로, 세계 패스 분리는 이미 존재합니다. 착수 전에 잰 것도 기록해 둡니다: "지도자를 바꿨다고 쓰고 polityChanges 없음" 같은 날카로운 서술-상태 불일치는 전 캠페인 167개 이벤트에 2건뿐이라 전용 호출감이 아니었습니다.

남은 것 중 실익이 확인된 순서:

1. **오래 묵은 명령 전용 호출.** 11라운드에 정밀 측정: 잔류 5건 중 4건은 모델이 이벤트를 아예 안 썼고, 커버리지 점수는 신호가 없었습니다(이행 0.400 대 방치 0.750 — 원칙 5). N턴 이상 남은 명령은 별도 호출에서 "이행하거나 왜 못 했는지 써라"를 강제하는 것이 답이고, 이는 원안의 1단계와 같은 모양입니다. 도메인 게이트는 켜면 안 됩니다(실측: 10개 이벤트 중 Technology & Research 분류 0개 — 그 도메인 명령은 후보 자체가 사라짐).
2. **일반 이벤트의 서술-상태 불일치 감시.** 연표 항목은 `effect`가 막지만 일반 이벤트는 아직 서술만 하고 상태를 안 움직일 수 있습니다. 빈도가 낮아(위 2건) 전용 호출 대신, C의 원장이 생기면 "서술된 변화 대 기록된 변화" 대조가 싸게 가능해집니다.
3. 2(외교 결과 반영)·4(상세화)·5(반응 이벤트)의 분할은 품질 병목이 다시 확인되면.

### E. 외교 개편

국가별 상호관계 수치(전략·경제적 유용성, 신뢰도, 호감도), 비밀회담/공개회담 구분(비밀회담은 미참여국의 이벤트 생성에 반영되지 않고 접근 불가), 외교 정보 종류(수교·전쟁·휴전·FTA), 다자방위체계. 성격 벡터가 이미 있어 토대는 깔려 있습니다 — 지금은 기질만 있고 관계가 없습니다.

**배치 검증도 이걸 기다립니다.** 미국 사드 포대가 개성(북한)에, UAE 관제 센터가 부산 앞바다에 선 실측 사례는 거리 휴리스틱으로는 못 잡습니다 — 미국 시설이 한국에 있으면 정상이고 북한에 있으면 오답이라는 판정은 동맹 관계를 알아야 가능합니다. 관계 수치가 생기면 "소유국의 우방 영토는 정상, 적성국 영토는 오배치"로 검증이 한 단계 올라갑니다.

### F. 프리셋 이식 · 역사적 판도

OpenHistoricalMap(CC0)을 활용한 시대별 행정구역 실현 — 상세 설계·실측·열린
문제는 **docs/PLAN-F-OHM.md**가 단일 출처다. 현황(2026-08-09): F-1 추출·F-2
조립·F-2.5 수리 완료(테스트 통과), **F-3(eraGeometry 프리셋 접합)은 Cowork
레인 대기**. 열린 문제 6건에 대한 클로드 코드 리뷰 회신이 같은 문서에 있다.
한편 Tier A(현대 GADM 근사) 프리셋 함대는 14종으로 마감(1444·1650·1804·1836·
1914·1935·1939·1946·2000·기원전 1200·117·1200·1300·현대).

### G. 그래픽 · UI · 폰트 (Phase 4)

지도는 기능적으로는 고쳤지만 보기 좋게는 아직 아닙니다.

**구조물·유닛 아이콘이 텍스트 글리프입니다.** ▲와 ■는 폰트에 반드시 있다는 이유로 고른 것이고 조악합니다. 종류별 스프라이트(기지·항만·발전소·연구소·통신)로 바꾸면 종류 추론 작업이 그대로 살아납니다.

**서울권이 뭉갭니다.** 유닛 배지, 구조물 글리프, 구조물 이름, 도시 이름이 겹칩니다. 유닛 배지는 충돌 회피를 꺼놓아(`text-allow-overlap: true`) 다른 것들을 덮습니다. 클러스터링이나 줌 단계별 개방이 필요합니다. (구조물 통폐합 80 → 44로 당장의 밀도는 낮아졌습니다.)

**중국 도시는 여전히 빽빽합니다.** 화면 공간 기준으로 솎고는 있지만 z3.6에서 조밀합니다. 근본적으로는 도시 데이터의 등급 재산정이 답입니다.

**국가 라벨 타이포그래피.** 곡선 라벨은 있지만 크기·자간·외곽선이 원본만 못합니다. 한글 폰트 선택 포함.

**색상 대비.** 옅은 색 국가가 지형과 구분이 잘 안 됩니다. 특히 하이라이트·선택 상태에서.

**남은 콘솔 경고.** hillshade와 3D terrain의 소스 공유, `elevation maxzoom > source.maxzoom`. 렌더 품질에 직접 영향이 있습니다.

---

## 3. 관찰 항목

**이름-좌표 불일치 — 사례 3건, 다음 후보.** "말라카 해협 인근" 관제 센터가 부산 앞바다에, "제주" 방어 구역과 "제주" 바이오-양자 허브가 강원도(400km 오차)에 있었습니다. 이름이 말하는 장소와 좌표를 대조하는 검증인데, "말라카 인근"(위치)과 "동남아 시장 타겟"(대상)을 구분해야 해서 오탐 위험이 있습니다. 사례가 더 쌓이면 지명 사전(현지화에 쓰는 것과 동일)으로 이름 속 지명을 뽑아 좌표와 대조하는 형태로 만듭니다.

**세계 이벤트 비율.** 초기 20~29%에서 최근 세 턴 30~38%로 올라왔습니다(목표는 절반 안팎). 연표 주입과 Already-Reported 목록이 든 뒤의 수치라, C(원장) 이후 재평가합니다.

**성격 시딩이 퍼지지 않음.** 프로필이 실제로 설정된 나라가 대한민국뿐인 상태가 이어졌습니다. 지시문은 매 턴 몇 개씩 확정하라고 되어 있는데 모델이 자국만 건드립니다. 성격을 난이도로 숨긴 만큼 국가별 차이가 실재하는지가 더 중요해졌습니다 — E(관계 수치) 작업 때 같이 봅니다.

**이벤트 kind 어휘 발산.** `plot` 11건, `mil11a` 1건 등 모델이 새 kind를 발명합니다. UI는 advance만 특별 취급하므로 실해는 없지만, kind별 표시(G의 스프라이트)를 하려면 마커 종류처럼 정규화가 필요해집니다.

**환경.** Ollama/CUDA 크래시(`0xc0000409`, `CUDA error: shared object initialization failed`)가 간헐적으로 납니다. 코드가 아니라 드라이버·VRAM 쪽입니다.

**하드웨어 대책.** 턴이 무거워지는 것의 답은 C(원장)와 D(분할)입니다. 모델을 키우는 것보다 효과가 큽니다.

---

## 4. 검증 현황

저장소 테스트 151개(server 26 + src 125), 이 작업에서 추가한 테스트 292개. 지도·프롬프트 예산·ops 스키마(클램프·op 드롭 포함)·번역·성격·행동 커버리지·standing 가드·통계 시트·마커 종류·연표(선택·예지 게이트·effect 주입·리비전 갱신·전체 대조)·배치 검증(국경 가드·나선 가드)·지명 현지화·호칭 제거·반복 필터 창·구조물 요약을 덮습니다. lint 0 errors.

검증 방식의 원칙: 실제 세이브의 문자열·좌표·이벤트를 그대로 재현 입력으로 씁니다 — "10라운드가 만든 impacts 전부 빈 이벤트"를 넣고 12월 턴에서 권한대행이 `countryStats`에 들어가는 데까지, "위도 95로 죽은 턴"을 넣고 두 이벤트가 다 살아남는 데까지. 임계값·창 크기 변경은 전 캠페인 재생으로 이득과 손실을 함께 셉니다. 시각 변경은 실행 화면으로 이중 확인합니다 — 페인트 표현식만 읽다가 두 번 놓쳤습니다.

턴마다 세이브를 직접 열어 콘솔이 말하지 않는 것(임팩트 빈 배열, 좌표, 라벨)까지 봅니다 — 이 문서의 실측 수치 전부가 그 산물입니다.
