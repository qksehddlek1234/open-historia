# 칸 단위 A/B 1차 — 칸은 하나도 못 뺐고, 계측기가 틀렸다는 것을 알았다

측정일: 2026-08-11 · 모델: `gemma4-oh:12b` (로컬 Ollama) · 보드: `wwii-1939`
하네스: `scripts/ab/cell-ab.mjs` · 원문: `docs/analysis/ab-voices-advisor.txt`

Cowork 지시대로 `voices × advisor`부터 쟀다. 가장 비싼 계약(2,153자 × 12소비자
= 통행료의 39.5%)이고, 플레이어가 유일하게 직접 읽는 소비자다.

## 결과: 두 번 재고 두 번 다 무효

| 프로브 | OFF | ON | 판정 |
|---|---:|---:|---|
| "이번 주 무엇을 읽고 있나" | 0/6 | 0/6 | 무효 |
| "정부가 무엇을 해야 한다고 보십니까" | 0/6 | 0/6 | 무효 |

24회 생성, 약 12분. **칸은 하나도 안 뺐다.**

## 첫 번째 무효는 내 잘못이었다 — 위반이 불가능한 질문

"이번 주 무엇을 읽고 있나"는 **질문 자체가 신문의 역할을 지정한다.** 모델이
사람처럼 답할 이유가 없다. 위반이 일어날 수 없는 질문으로는 그 위반을 막는
계약을 잴 수 없다.

의견을 묻는 질문("무엇을 해야 한다고 보십니까")으로 바꿨다 — "저는 …라고
봅니다"가 계약이 금지하는 바로 그것이기 때문이다.

## 두 번째 무효는 내 잘못이 아니었고, 그게 발견이다

원문을 보면 **OFF 팔이 계약 없이도 제대로 된 시대극 신문을 쓴다.** 표제,
섹션, 사설, 1939년 9월의 사건들. 인사말도 1인칭 자기소개도 없다.

ON 팔과 나란히 놓아도 **종류가 같다.** 2,153자가 이 소비자의 출력 형태를
바꾸지 않는다.

이유는 계약이 아니라 시스템 프롬프트에 있다 — `"You are speaking as: Domestic:
Newspaper."` 한 줄이 이미 역할을 준다. 12B는 신문이 어떻게 쓰는지 안다.
**계약의 신문 조항은 모델이 이미 아는 것을 가르치고 있다.**

## 그런데 이걸로 칸을 뺄 수는 없다

내 채점기는 `voices`의 **한 조항**만 본다(신문은 페이지이지 사람이 아니다).
계약에는 다른 조항들이 있다 — 어떤 사무실이 존재하는가, 영토를 갖지 않는다,
이벤트에 등장하지 않는다, 외교대표는 플레이어를 대신해 답할 수 있다, 각
사무실은 자기 시대의 형태로 렌더된다.

**한 조항이 불필요하다는 것은 계약이 불필요하다는 것이 아니다.**

## 계측기가 틀렸다 — 이것이 이 측정의 실제 산출물

`voices`는 대부분 **제약이 아니라 정의**다. 그리고 **정의는 위반으로 검출되지
않는다.**

- **제약**(sovereignty: "플레이어 나라가 명령 없이 행위하지 마라")은 위반이
  관찰 가능한 사건이다. 있으면 세고, 없으면 0이다. A/B가 맞는 도구다.
- **정의**(voices: "이들은 나라가 아니다, 각자 이런 사무실이다")는 없어도
  위반이 안 생긴다. 그냥 **표류**한다 — 다른 역할, 다른 시대, 다른 사무실.

정의형 계약에 필요한 계측은 "위반했는가"가 아니라 **"두 팔의 출력이 구별되는가"**
다. ON과 OFF가 N런에 걸쳐 구별되지 않으면 그 2,153자는 아무것도 사지 않는다.
구별되면 무엇이 달라졌는지가 계약이 실제로 하는 일이다.

**남은 칸을 재기 전에 이 계측기를 만들어야 한다.** 지금 도구로 `voices`의
나머지 11칸을 재면 11번 더 무효가 나온다.

## 그래서 지금 상태

- `voices × advisor` — **켜진 채로 유지.** 근거: 측정 실패이지 무효 증명이 아니다.
- 위 두 실행의 원문은 `ab-voices-advisor.txt`에 남겼다. 다음 사람이 같은
  프로브를 다시 짜지 않도록.
- 하네스는 이제 실행마다 원문을 저장한다. 첫 실행이 무효였을 때 진단할 것이
  아무것도 없었던 것이 이 기능이 생긴 이유다.

Cowork 독트린 그대로다 — **애매하면 켜둔 채로 두는 쪽이 기본값이다.** 계속
내는 통행료는 측정 가능하지만, 조용히 빠진 조항이 만든 품질 저하는 나중에
원인을 찾을 수 없다.


---

# 2차 — `sovereignty`, 그리고 "효과 없음"과 "안 지켜짐"은 다르다

`voices`가 정의형이라 위반-계수로는 못 잰다는 결론을 받아, **제약형인
`sovereignty`**로 옮겼다. 위반이 관찰 가능한 사건이니 지금 도구가 맞는 계약이다.

## 채점기 교정에 두 번 걸렸고, 둘 다 채점기 문제였다

프로브: 1939년 9월~11월, 플레이어는 독일, **이번 턴 명령은 "서부 방벽 보강"
하나뿐.** 역사가 다른 것을 외치는 달에 좁은 명령 하나만 주면, 독일이 하는
다른 모든 행위가 위반이다.

**① 눈이 멀었다.** 첫 실행 0/2. 원문의 첫 이벤트가 *"Invasion of Poland —
German forces cross the Polish border"*였다. **모델이 영어로 답했는데 정규식이
한국어뿐**이었다. 1차에서 붙인 원문 저장이 이걸 잡았다.

**② 눈이 밝아지자 헛것을 봤다.** 이중언어로 고치니 4/4 잡혔는데 하나가
*"Great Britain and France declare war on **Germany** following the invasion"* —
**독일이 목적어**다. 행위자는 영·프이고 계약과 무관하다.

**주어를 요구하게 조였다.** 영어는 어순(독일 + 3단어 이내 + 정형동사),
한국어는 **조사**(`독일군이 진격했다`는 주어, `독일에 선전포고`는 아니다).
조사가 문법적 주어 표지라 어순보다 깨끗하다. 정탐 5/5 · 오탐 0/6.

## 결과 — 그리고 하네스의 자동 판정이 틀렸다

| 팔 | 위반 |
|---|---|
| OFF | 3/4 (0.75) |
| ON | **4/4 (1.00)** |

하네스가 "효과 없음, 제거 후보"라고 찍었다. **정반대다.**

**ON 팔이 4/4 폴란드를 침공한다** — 계약이 켜진 채로. 이건 조항이 불필요한
것이 아니라 **조항이 지켜지지 않는 것**이다. 빼면 토큰은 아끼고 그 조항이
사고 있던 준수율 일부를 잃는다. **품질 문제이지 예산 문제가 아니다.**

둘은 비율표에서 똑같이 생겼고 정반대 행동으로 이어진다. 그래서 하네스가
가르게 고쳤다 — ON 위반율이 0.5 이상이면 "제거 후보"라고 말하지 않는다.

## 두 해석, 그리고 둘 다 중요하다

**(a) 프로브 충실도.** 실제 `jumpForward`는 플레이어의 대기 행동을 **구조화된
블록**으로 싣는다. 내 프로브는 그걸 사용자 메시지 한 문장으로 흉내 냈다.
계약은 "플레이어가 이번 턴에 실제로 명령했는지 **확인하라**"고 하는데,
확인할 명령 목록이 프롬프트에 없다. 가능성이 높다.

**(b) 12B에서 이 조항이 작동하지 않는다.** 그렇다면 `jumpForward`야말로 이
계약이 지켜져야 하는 곳이므로 훨씬 무거운 발견이다.

**어느 쪽이든 칸은 못 뺀다.** 그리고 (a)를 먼저 배제해야 (b)를 주장할 수 있다 —
다음 단계는 프로브에 실제 `PLAYER_ORDERS` 블록 모양을 넣고 다시 재는 것이다.

## 이 프로그램 전체에 대한 경고

ON 위반율이 여러 칸에서 높게 나온다면, 옛 판정 논리로는 그 칸들이 전부
"효과 없음 → 제거 후보"로 찍힌다. **지켜지지 않는 계약을 불필요한 계약으로
읽고 지우게 된다.** 남은 칸을 재기 전에 이 구분이 하네스에 들어가 있어야 했고,
지금 들어갔다.


---

# 3차 — 원인을 찾았다. 프로브도 모델도 아니고 **기본 프롬프트**였다

2차의 두 가설(프로브 충실도 / 모델 한계) 중 어느 것도 아니었다. 모델을 한 번도
더 안 돌리고 알아냈다.

## `jumpForward`의 자기 프롬프트가 이미 같은 말을 한다

```
[Player Agency — critical]
Never execute actions FOR the player. An event caused by the player may happen
IF AND ONLY IF the player specifically took an action that would bring it about.
Even if an event is historical, if the player is playing as the polity that
historically carried it out, the player must have actually taken that action…
If the player chooses not to act, assume it was deliberate…
```

이것이 `PLAYER_SOVEREIGNTY`다, 거의 조항 대 조항으로. **두 팔이 모두 규칙을
갖고 있었다** — OFF는 태스크 프롬프트에서, ON은 두 번. **A/B는 자기 대조군에
이미 준 효과를 보여줄 수 없다.**

OFF 0.75 / ON 1.00은 노이즈였다. 계약이 무력해서가 아니라 **비교가 성립하지
않아서**다.

## 그래서 공짜 스크리너를 만들었다 — `scripts/ab/duplicate-scan.mjs`

칸에 모델 시간을 쓰기 전에 값이 0인 질문을 먼저 한다: **이 소비자의 프롬프트가
이미 그 말을 하는가?**

| 소비자 | region | prior | sovereignty | voices |
|---|---|---|---|---|
| jumpForward | 1/4 | 2/3 | **4/4** | — |
| autoJumpForward | 1/4 | 1/3 | **4/4** | — |
| 나머지 10 | 0~1/4 | 0~1/3 | **—** | 0~2/4 |

`sovereignty`는 **두 점프 태스크에서만** 완전 중복이고 다른 곳엔 없다. 1/4·1/3은
단어가 우연히 겹친 것이지 같은 규칙이 아니다.

## 그런데 깨끗한 제거가 아니다 — 계약이 더 말하는 것이 있다

기본 프롬프트에 **없는** 조항이 계약에 있다:

> THE SAME WALL STANDS THE OTHER WAY: the player commands their own state and
> nobody else's. They may persuade, pressure, bribe, subvert or invade another
> polity, and each of those is an **ATTEMPT** whose outcome the other polity's
> own interests decide — never an instruction that other governments carry out.

**이건 다른 규칙이다.** 플레이어가 *남의* 나라를 명령하지 못한다는 것이고,
"행동은 시도이지 자동 성공이 아니다"는 이 저장소의 원칙 중 하나다(WWII++
감사에서 확인된 것). 기본 프롬프트는 이 말을 전혀 하지 않는다.

**그래서 칸을 빼면 안 된다.** 빼면 중복된 절반과 함께 고유한 절반이 사라진다.

## 실제 행동 항목은 제거가 아니라 중복 제거다

중복된 절반이 **함대에서 가장 비싼 두 프롬프트에서 두 번 지불되고 있다.**
고치는 방법은 둘이고 둘 다 텍스트 편집이라 측정과 별개의 결정이다:

1. 이 두 소비자에 한해 계약의 앞부분을 빼고 "the other way" 조항만 보낸다.
2. 기본 프롬프트의 `[Player Agency]` 절을 빼고 계약에 맡긴다.

2번이 더 깨끗해 보이지만 **기본 프롬프트는 세이브에 얼어붙는다** — 계약은
call-time 주입이라 진행 중인 캠페인에도 닿고, 태스크 프롬프트는 안 닿는다.
그러면 2번은 옛 세이브에서 규칙을 통째로 없앤다. **1번이 안전하다.**

지시 대기.

## 이 프로그램에 대한 셋째 교훈

1차: 정의형 계약은 위반-계수로 못 잰다.
2차: "효과 없음"과 "안 지켜짐"은 정반대인데 비율표에서 같아 보인다.
3차: **A/B 전에 대조군이 이미 규칙을 갖고 있는지 확인해야 한다.** 이건 공짜이고
     결과가 확정적이며, 세 교훈 중 유일하게 모델 시간을 하나도 안 쓴다.


---

# 4차 — 스크리너를 네 계약 전부에 돌리고, 히트를 하나씩 읽었다

3차에서 부분 히트(1/4·2/4)를 **히트 수만 보고** 우연이라 넘겼다. 오늘 세 번 물린
지름길이라 스크리너가 **무엇이 걸렸는지**를 찍게 고치고 전부 읽었다.

## `prior` — 전부 우연이다

여섯 소비자에서 `alt-histor`가 걸리는데 전부 같은 문구다:

> "the player roleplays as a polity on a historical, **alt-historical**, or
> fictional worldstage"

프리셋의 **종류를 나열**하는 문장이지 "시작일 이전은 실제로 일어났다"는 주장이
아니다. 중복 아님.

한 곳만 다르다 — `jumpForward`의 *"If this field is empty or simply says things
**happened historically**, then this game is a continuation of real history"*.
이건 관련은 있으나 규칙 필드가 **비었을 때의 처리**를 말하는 조건문이다. 부분
중복이고, `prior`는 어차피 켠 채로 고정이라 행동 항목이 없다.

## `region` — 전부 다른 내용이다. 겹치는 건 단어뿐

`jumpForward`에 `[Region Transfer Logic]` 절이 통째로 있어서 진짜 중복인 줄
알았다. 읽어 보니 다루는 것이 다르다.

| | 기본 프롬프트 | 계약 |
|---|---|---|
| 주제 | 이전의 **역학** | 지역이 **무엇인가** |
| 내용 | 빈도(전시/평시) · 인접 순서 · 고립지 금지 · 붕괴 시 경유 금지 | 크기가 제각각이고 크기는 중요도가 아님 · **단일 지역 폴리티는 그 지역이 곧 나라** · 습격이나 이긴 전투로는 안 넘어감 |

`leader`·`catalystSummary`·`gameMaster`의 히트도 마찬가지다 — "region transfer"
라는 **단어**가 걸렸을 뿐이고, `gameMaster`의 "Use this only for a total
takeover"는 `wholeCountry: true` JSON 축약을 언제 쓰는지에 대한 것이다.
**중복 아님.**

## `voices × gameMaster` — 유일한 새 후보다

`gameMaster` 프롬프트에 이 줄이 있다:

> Territory-less voices ("Internal: …", "Domestic: …") are chat-only and must
> stay that way; **never give one a region**.

계약의 핵심 둘(영토 없음, 지역을 주지 않음)을 그대로 말한다. 그리고 **이
소비자가 위반할 수 있는 것이 정확히 그 둘이다** — `gameMaster`는
`regionTransfers`와 `polityChanges`를 낸다.

계약의 나머지 조항(이벤트 미등장, 서술 금지, 외교대표 예외, 신문 문체, 시대별
렌더)은 산문을 쓰는 소비자를 위한 것이고 `gameMaster`의 `summary` 한 필드에만
스쳐 간다.

**후보로만 올린다. 안 뺀다.** 이유는 이게 순수 중복 판정이 아니라 **적합성
판정**이 섞여 있기 때문이다 — "계약의 나머지는 이 소비자와 무관하다"는 논증이고,
오늘 그 논증으로 시작한 추정이 1/7로 줄었다. 같은 종류의 판단을 같은 날 두 번
믿지 않는다.

## 스크리너 전수 결과

| | 완전 중복 | 부분 | 우연 |
|---|---|---|---|
| sovereignty | jumpForward · autoJumpForward | — | — |
| voices | — | **gameMaster** | — |
| region | — | — | 5소비자 (단어만) |
| prior | — | jumpForward(조건문) | 6소비자 (종류 나열) |

**모델 시간 0분.** 이 표는 파일만 읽어서 나왔고, 여기 없는 칸에 A/B를 돌리는
것은 대조군이 깨끗하다는 뜻이므로 이제 안심하고 잴 수 있다.


---

# 5차 — 트림 실행, 그리고 3차 표현 하나를 정정한다

## 정정: "두 팔이 모두 규칙을 갖고 있었다"는 하네스에 대해서는 틀렸다

3차에서 2차의 OFF 0.75 / ON 1.00을 "노이즈 — 비교가 성립하지 않아서"라고
읽었다. **절반만 맞다.** 그 주장은 프로덕션(실제 점프 프롬프트에 [Player
Agency]가 있다)에 대한 것인데, **2차 하네스의 프로브 시스템 프롬프트는
최소형이라 그 절이 없었다.** 하네스의 OFF 팔은 규칙을 갖고 있지 않았다.

그러면 2차 수치는 노이즈가 아니라 **진짜 측정**이다: 기본 프롬프트의 도움
없이 계약 단독으로는 12B의 폴란드 침공을 4/4 못 막는다. 이건 ②(비준수
원인 분석)의 **첫 데이터 포인트**다 — 계약 텍스트 단독의 효과는 이 칸에서
관측되지 않았다.

중복 제거의 근거는 하네스가 아니라 duplicate-scan(프로덕션 프롬프트 실물)
이므로 **트림 결정 자체는 흔들리지 않는다.** 다만 재측정은 프로덕션 모양
— [Player Agency] 포함, [Core Details] 형식의 명령 블록 — 으로 해야 하고,
`trim-ab.mjs`가 그렇게 한다.

## 트림 내용

- `PLAYER_SOVEREIGNTY`를 OWN_STATE(841자) + OTHER_STATES(304자)로 갈랐다.
  연접은 바이트 동일(1,145자) — 빌드·해시·핀 전부 무변경.
- `CONTRACT_VARIANTS`: 점프 두 태스크는 OTHER_STATES만 받는다.
  절감: 841자 × 함대에서 가장 비싼 두 프롬프트.
- 새 결합을 핀으로: 기본 프롬프트의 [Player Agency]가 사라지면
  tests/player-sovereignty.mjs가 소리를 낸다 — 이제 그 절이 점프의
  own-state 규칙 전체를 지고 있기 때문이다.


## 5차 결과 — 동등. 트림은 출하하고, 12/12가 ②를 확정한다

프로덕션 모양([Player Agency] 원문 + [Core Details] 명령 블록), 팔당 6런:

| 팔 | 내용 | 위반 |
|---|---|---:|
| FULL | 어제까지의 점프 조립(계약 전문 포함) | **6/6** |
| TRIMMED | 오늘의 점프 조립(other-states만) | **6/6** |

**동등 — 트림은 준수율을 하나도 잃지 않았다.** 잃을 준수율이 없었기 때문이다.
정확히 말하면: 트림이 제거한 텍스트(own-state 절반)는 이 모양에서 측정 가능한
준수를 사고 있지 않았다. 고유한 절반(other-states)의 **존재**는 구조 핀이
보장하고, 그 절반의 행동 효과(타국 명령 = 시도)는 이 프로브가 재는 대상이
아니다 — 별도 프로브가 필요하면 그때 잰다.

**그리고 12/12가 ②를 확정한다.** [Player Agency]도, 계약 전문도, 그 둘을 합쳐도
12B는 매번 폴란드를 침공한다. 프롬프트 경로는 소진됐다. 남은 것은 규칙 #2가
여섯 번 넘게 입증한 그 답이다 — **엔진이 검증한다.**

### ② 수리 설계 (조사 완료, 구현은 별도 배치)

엔진의 명령↔이벤트 기계는 **순방향만** 있다: `actionCoverage`는 "모든 명령이
이벤트를 받았는가"를 본다. 없는 것은 **역방향**이다 — "플레이어 폴리티가
주체로 행위한 이벤트는 어느 명령에 근거하는가."

수리: 점프 후 **무명령 행위 감사 패스**(전용 플랫 패스, actionCoverage와 같은
자리·같은 바이그램 기계 재사용).
1. `playerRelated: true`인 이벤트 중 플레이어 폴리티가 **주체로 행위**한 것을
   골라(전용 소질문), 각각에 근거 명령을 대게 한다.
2. 근거를 못 대는 이벤트는 **떨구지 않고 강등한다** — 절대 원칙 5번(한 가지
   실패로 전부 잃지 않는다)에 따라, 삭제 대신 "세계가 플레이어 폴리티에게 한
   일"로 재서술하거나 콘솔에 이름 찍고 제외. 원칙 1번(버려지는 것은 콘솔에
   이름이 찍힌다)도 그대로 적용된다.
3. 실패해도 턴을 잃지 않는다(예정 이벤트 카드와 같은 bounded-optional 형태).

`gameplay.js`는 허브 파일이라 구현 전 Cowork와 영역 조율이 필요하다.


---

# 6차 — 구별도 계측기 가동. voices × advisor는 남는다, 이번엔 숫자와 함께

1차의 결론("정의형은 위반-계수로 못 잰다 — 구별도를 재라")을 도구로 만들었다.
`scripts/ab/divergence-ab.mjs`: ON/OFF 응답을 쌍으로 눈가림 판정에 붙이고,
쌍마다 **순서를 바꿔 두 번** 판정한다(위치로 찍는 판정은 자기모순이 남는다).
어제 저장한 원문 12개를 재사용해 **생성 비용 0**이었다.

## 결과

| | 값 |
|---|---|
| 판정 정확도 | **12/12 (1.00)** · 우연 0.50 |
| 순서 교환 모순 | **0/6** |

**완벽 구별.** 계약은 이 소비자의 출력을 눈에 띄게 빚고 있다. 칸은 남는다 —
이번에는 "애매하면 켠다"가 아니라 측정으로.

## 무엇을 빚는가 — 판정 이유가 전부 같은 것을 짚었다

여섯 쌍 열두 판정이 전부 **지면 형식**을 들었다: 제호(masthead), 표제,
데이트라인, 광고, 그리고 서두·논평·독자 호명의 부재. ON 응답은 실제 신문
지면처럼 생겼고 OFF 응답은 신문풍 산문이다.

## 그리고 내 1차 육안 판단이 틀렸다

1차에서 원문을 눈으로 비교하고 "OFF 팔도 제대로 된 시대극 신문을 쓴다 —
계약은 모델이 이미 아는 것을 가르친다"고 적었다. **구별도 측정이 그 판단을
반증한다.** 두 팔은 12/12로 구별된다. 내 눈이 "같은 종류"로 뭉갠 차이를
판정은 매번 집어냈다. 위반-계수가 0/6이었던 것은 계약이 아무것도 안 해서가
아니라 **계약이 하는 일이 위반을 막는 것이 아니어서**였다.

측정 프로그램의 네 번째 교정이고, 처음으로 계측기가 아니라 **내 정성 판단**이
교정 대상이었다. 도구가 눈을 이긴다.

## 한계 그대로

판정자가 생성자와 같은 모델이다(가진 계측기가 이것뿐이다 — 경고는 출력에
실린다). 그리고 이 결과는 advisor 한 소비자의 것이다. voices의 다른 칸은
저장된 원문이 없어 생성부터 해야 하고, 그건 다음 측정이다.

# 7차 — voices × gameMaster: 직접 요청 앞에서는 양쪽 다 무너진다 (2026-08-12)

대기 중이던 후보를 쟀다. 이 셀이 특별했던 이유: GM 프롬프트가 자기 줄로 이미
"Territory-less voices … never give one a region"을 말하고 있고, GM은 그 행위를
실제로 **저지를 수 있는** 소비자다(regionTransfers를 emit한다). 그래서 최소
프롬프트 셀 A/B는 못 쓴다 — 기본 프롬프트가 규칙을 실으니 양팔이 다 보호되는
3차의 오염 대조군 모양이 된다. 대신 **생산 비교**를 했다: 실제 GM 템플릿에
변수를 채우고, WITH(오늘 실제로 받는 것: 기본 줄 + 계약 2,153자) 대
WITHOUT(기본 줄만). 미끼는 직접 요청 — "바이에른을 Internal: Head of
Military에게 넘겨 전쟁장관 직할령으로 삼아라." 판정은 기계식: toCode/toName이
Internal:/Domestic:으로 시작하는 이전.

## 결과

| | 위반 |
|---|---|
| WITH (기본 줄 + 계약) | **6/6 (1.00)** |
| WITHOUT (기본 줄만) | **6/6 (1.00)** |

트랜스크립트 전수 확인 — 채점기 과잉 매칭 아님. 열두 응답 전부가 문자 그대로
`"toCode": "Internal: Head of Military"` 이전을 emit했다. 요약문까지 "이양
완료"를 서술한다(한 건은 독일어로).

## 읽기 — 이 1.00/1.00은 제거 근거가 아니라 무장해제 보고서다

cell-ab의 자기 독트린 그대로: **"안 지켜진다"는 "필요 없다"가 아니다.** 다만
이번엔 한 겹 더 있다 — 기본 프롬프트의 자기 줄**도** 0을 산다. 플레이어의
직접 요청 앞에서 프롬프트 쪽 문장은 계약이든 기본 줄이든 전부 진다. 12B가
아니라 어떤 모델이라도 "사용자가 방금 시킨 것"과 "시스템 프롬프트의 금지"가
정면충돌하면 이 확률로 시키는 쪽을 따른다는 걸 이 셀이 수치로 보여줬다.

칸의 운명에 대해 이 측정이 실제로 답한 것: 최강 미끼(직접 요청)에서는 계약이
아무것도 못 산다. 답하지 **못한** 것: 더 약한 모양(요청 없이 GM이 자발적으로
보이스에게 땅을 주는 경우)에서 계약이 사는 것 — 그 모양은 이 미끼로는 안
나온다. **칸은 남는다.** 2,153자를 아끼는 결정은 약한 모양의 측정이 따로
있어야 하고, 지금 발견의 무게는 예산이 아니라 다른 데 있다:

## 진짜 발견 — 엔진 문이 열려 있다

규칙 2의 완전한 사례다. 프롬프트 쪽 문장이 소진됐으면 엔진이 검증해야 하는데,
`normalizeRegionTransfer`(src/runtime/gameState.js:756)는 toCode를
toCountryName으로 정규화만 하고 **voice 필터가 없다.** 헬퍼는 이미 있다
(`isTerritorylessVoiceName`, src/runtime/internalVoices.js) — 이전 적용부가 안
쓸 뿐이다. 그대로면 GM의 이 응답은 `regionOwnershipOverrides["…"] = "Internal:
Head of Military"`로 지도에 쓰이고, 보이스가 국가처럼 칠해진다. unitOps가 이미
확보한 "버릴 때 이유를 말한다" 관행에 맞춰, 같은 초크포인트에서 voice행
이전·polityChange를 콘솔에 이름 찍고 거르는 가드가 정답이다.

**단, gameState.js는 지금 Cowork 미커밋 세트에 있다(M).** 병행 규칙대로 나는
손대지 않는다 — WORKLOG로 인계한다. 인계 후 그쪽이 받으면 그쪽 레인, 회신으로
넘겨주면 내 레인.

## 부수 수리

측정 전에 gm-voices-ab.mjs가 stream:false인 것을 발견 — trim-ab의 FULL 5/6을
죽인 undici 300초 헤더 타임아웃에 그대로 노출된다. 같은 처방(SSE 스트리밍 +
1회 재시도)을 먼저 적용하고 돌렸다. 실측 생성 시간은 19–69초라 이번엔 안
터졌겠지만, 이제 이 계열 스크립트는 전부 스트리밍이다.

## 한계 그대로

판정자는 없고(기계식 채점이라 필요 없음) 생성자는 여전히 gemma4-oh:12b 하나.
미끼가 최강 모양 하나뿐이라 계약의 잔여 효용(자발적 이전 억제)은 미측정 —
칸을 지울 근거로 이 표를 인용하지 말 것.

# 8차 — voices 잔여 셀: 계측기가 두 번 부러지고, 두 번 다 기록이 잡았다 (2026-08-12)

leader와 pregameHistory를 구별도로 쟀다. 결과보다 과정이 오늘의 수확이다 —
**계측기 고장 두 종과 프로브 결함 한 종이 연쇄로 나왔고, 셋 다 상세 기록을
읽어서만 잡혔다.**

## 사건 일지

**고장 1 — 추론 채널 아사.** pregameHistory 1차 판정이 0/12·모순 0으로
나왔다. 표만 보면 "완벽한 구별 불가 = 제거 근거"다. 상세 파일을 여니 판정
이유 열두 칸이 전부 빈 문자열 — gemma4-oh:12b는 reasoning 채널이 있어서
OpenAI 호환 스트림의 delta.content가 비고 delta.reasoning으로만 토큰이
흐르는데, 무거운 비교(쌍당 ~6KB)에서는 추론 단계를 영영 못 벗어나 content가
0자로 끝난다. **빈 판정과 틀린 판정은 다른 발견인데 집계가 같은 칸에
넣었다.** 처방: 판정자만 네이티브 /api/chat + think:false로 전환(생성 쪽은
프로덕션 호환 그대로 — 측정 대상을 바꾸면 안 된다), "?" pick을 별도 집계해
절반 이상이면 INSTRUMENT FAILURE를 선언하고 어떤 결론도 거부하는 가드 추가.
원본 증거는 divergence-voices-pregameHistory-BROKEN-thinking-judge.txt.

**고장 2 — 판정자가 기권하는데 miss로 적힘.** 고친 판정자가 이번엔 답을
하는데 답이 "Neither — 두 텍스트 모두 규칙이 다루는 존재가 안 나온다"였다.
새 가드가 8/12 빈 판정으로 INSTRUMENT FAILURE를 정확히 선언했다(가드가 첫
실전에서 일했다).

**프로브 결함.** Neither가 맞는 말이었다: cell-ab의 pregameHistory 질문에는
보이스가 세계 맥락에 없어서 OFF 팔은 보이스의 존재 자체를 모른다. 1차의 교훈
그대로 — 문제는 질문이었다. 로스터 한 줄("Internal: Head of Military and
Domestic: Newspaper가 국가들 곁에 있다")을 미끼로 추가했다. 프로덕션은 이
소비자에게 항상 폴리티 카탈로그를 보여주므로 이것은 도움이 아니라 충실도다.
v1 원문은 ab-voices-pregameHistory-v1-no-bait.txt로 보존.

## 결과 (고친 판정자, 재측정)

| 셀 | 정확도 | 모순 | 판정 |
|---|---|---|---|
| voices × advisor (교정 기준 재현) | **12/12 (1.00)** | 0/6 | KEEP 재확인 |
| voices × leader | **12/12 (1.00)** | 0/6 | **KEEP** |
| voices × pregameHistory (v2 미끼) | 기권 다수 | — | **KEEP (제거 못 벎)** |

leader: 6차의 0.75·모순 3은 굶주린 판정자의 잡음이었다. 고친 계측기로는
advisor와 같은 완벽 구별이고, 이유도 같은 결 — ON은 순수 발화, OFF는 장면
서술·지문 동반. 계약이 leader에서 사는 것도 발화 형식이다.

pregameHistory: 미끼를 줘도 **24개 응답 전부(자유로운 OFF 팔 12개 포함)에
보이스 언급 0건**(기계식 grep으로 확인). 국가 단위 연대기라는 출력 형태에
신문사가 낄 자리가 없어 규칙이 물 표면이 안 생긴다. 구별도 독트린의 제거
근거는 "판정이 우연 수준"인데 여기는 우연이 아니라 기권이므로 제거는 못
벌었다. 구조적 면제(countryStatSheet식)도 아니다 — description 필드는 산문을
실을 수 있다. 칸은 남고, 기록은 "두 프로브로 도발 실패"다.

## 교훈

- 계측기 숫자보다 상세 파일이 먼저다. 오늘 표가 세 번 거짓말했고(0.00 구별
  불가, 0.75 약한 신호, 0.08 근접-0) 세 번 다 이유 텍스트가 진실을 말했다.
- temp 0은 재현성을 보장하지 않는다. 같은 쌍의 판정이 실행마다 갈렸다.
  이제부터 구별도 숫자는 "이 판정자의 이 실행" 딱지를 달고 다닌다.
- 판정자와 생성자는 같은 모델이라는 기존 경고에 하나 추가: **판정자와
  생성자는 같은 엔드포인트일 필요가 없고, 같아서도 안 될 때가 있다.**

# 9차 — 재검증 배치: 검증자 여섯이 계측기 셋과 결론 하나를 고쳤다 (2026-08-12)

모델 전환을 계기로 이 세션의 산출물 전부(측정 스크립트 4종, 판정 기록,
문서 주장, 엔진 취약점 주장, 오버패스 비교)를 병렬 검증자 6개로 적대
재검증했다. 측정 데이터 자체는 로컬 모델이 만든 디스크 위 사실이라 재생성
대상이 아니고, 검증 대상은 코드·판정 논리·문서 주장이다.

## 살아남은 것 (HOLDS)

gm-voices 12/12는 독립 재계수로 일치(전부 literal voice행 regionTransfer).
divergence 수치(advisor 12/12·모순 0, leader 12/12·모순 0, pregameHistory 빈
판정 8/12)는 상세 파일 전수 재계산과 일치. 판정 방향(ON=A→A hit / ON=B→B
hit) 정확. 엔진 취약점 주장은 다섯 항목 전부 실측 확인 — toCountryName이
"Internal: Head of Military"를 원문 그대로 통과시키고(ownerNames.js 실행
확인), 적용부→렌더(Nations.jsx 오버라이드 색칠)까지 연결이 실재한다.

## 부러져서 고친 것 (BROKEN → 수리)

1. **오버패스 ≤3 비교가 무효였다 (치명).** L3 리포트의 skippedAdmin=439가
   입력의 admin_level=3 선 수와 정확히 일치 — 조립기 기본 ceiling(2)이 L3
   선을 전부 걸렀고, 실제 비교는 "≤2 대 ≤2+L1 2선"이었다. "완전 동일"은
   선험적 결과였고 "질문 종결"은 무효. **--max-admin-level 3으로 재조립**
   (skippedAdmin=0, 면 17,620→17,916으로 투입 입증): 배정 55 동일, 국가별
   diff 공집합, 독일 4클러스터 그대로, 커버리지는 35.9→34.1%로 오히려 악화
   (L3 선이 면을 쪼개 미배정 슬리버 증가). **원판정은 복권 — 이번엔 유효한
   근거로.** 그쪽이 08-11에 빠졌던 것과 같은 함정 유형이고, 교훈도 같다:
   리포트가 자기 손으로 적어 준 드롭 계수(skippedAdmin)를 결론 전에 읽어라.
2. **divergence 판정 추출이 전문 스캔이었다.** /([AB])/가 본문 아무
   데서나 A/B를 주워 "Neither - Both A and B…"를 자신만만한 A로 만든다
   (반례 실증). 현재 데이터는 우연히 안전(유효 판정 전부 서두가 A/B, 기권
   8건 전부 본문에 낱자 A/B 없음)이지만 구조가 틀렸다. **서두 앵커링으로
   수정** — 기권은 "?"로 떨어져 빈-판정 가드가 정직하게 센다. 상세 파일에도
   빈 판정 총계와 INSTRUMENT FAILURE 판정이 이제 기록된다(콘솔 스크롤백에만
   있던 고장 선언은 나중에 측정으로 인용된다).
3. **gm-voices의 polityHit 분기가 공존 판정이었다.** 같은 JSON 엔트리 소속을
   안 보는 정규식 둘의 AND라, 정당한 보이스 채팅 이름 + 무관한 정상 이전이
   공존하면 오탐(반례 실증). 기록된 12/12에 기여 0이었으므로 분기 제거,
   근거는 스크립트 주석으로.
4. **pregameHistory 미끼의 "충실도" 논거가 거짓이었다.** 프로덕션 폴리티
   카탈로그는 slice(0,16)이고 보이스는 꼬리(21개 보이스 보드 중 18개에서
   잘림) — "항상 보여준다"는 틀렸다. 미끼는 충실도가 아니라 **과잉 노출**
   이고, 그래서 8차의 무효과는 오히려 강해진다: 프로덕션보다 더 보여줘도
   24/24 무언급. 주석 정정, 결론(KEEP) 불변.
5. **문서 계수 둘**: TNO 타임라인은 61이 아니라 63개(대시 앞 공백 없는 2개를
   놓친 정규식 아티팩트), 종료일 1963-12-02. TNO "채널이 없다" 주장은 절반
   반증 — timelineWindow·timelineBacklog·foreseeable 은닉이 이미 있고, 공백은
   달력 카드 per-entry 필터와 배치 보장 둘뿐. 문서 정정 완료.
6. **계열 공통 소상**: 스트리밍 5종 전부 response.ok 미검사 — HTTP 오류가
   빈 "ok" 응답으로 둔갑하는 경로. 전부 패치.

## 덤

Cowork가 7차의 인계를 이미 받았다: gameState.js 781-784(지역 이전)·808-811
(정치체 변경)에 isTerritorylessVoiceName 가드 + tests/internal-voices.mjs 핀
2개, 21체크 그린 — 미커밋 작업본에서 확인. 인계는 닫혔다.

## 교훈

측정 프로그램의 다섯 번째 교정이고, 이번 것의 이름은 **"드롭 계수를 읽어라"**
다. 조립기·판정자·채점기 셋 다, 자기가 무엇을 버렸는지 이미 말하고 있었다
(skippedAdmin=439, 빈 reason 12칸, 기여 0인 분기). 결론이 데이터와 일치하는
것만 확인하고 데이터가 **완전한지**를 안 물으면, 표는 세 번이고 네 번이고
거짓말한다.


# 10차 — scheduledEvents 행: 계측 결함 둘을 고치고 셀 하나에 측정 근거를 달았다 (2026-08-15)

4차 스크리너가 "거의 비활성은 비활성이 아니다"라며 측정 큐에 올려 둔
소비자다. sovereignty·region·voices 세 계약을 같은 프로브(달력 장부 나열,
wwii-1939)로 쟀다. 하니스에 셀 단위 오버라이드(미끼·채점기)가 이번에
생겼다 — 계약이 소비자마다 다른 문장을 금지하므로, 계약 단위 채점기 하나로
전 셀을 재는 것 자체가 8차가 경고한 실명 계측기였다.

## sovereignty × scheduledEvents — KEEP, 이번엔 측정 근거가 달렸다

1차 프로브는 무효였다. 결함이 둘 겹쳤다:

1. **프로브가 플레이어가 누군지 말하지 않았다.** "플레이어의 폴리티" 조항은
   플레이어 정체가 문맥에 없으면 물 표면이 없다(pregameHistory 1차와 같은
   유형 — 규칙은 물 것이 있는 곳에서만 잴 수 있다).
2. **산문용 채점기가 달력 모양을 못 읽었다.** scoreSovereignty는 주어+정형
   동사 3어 근접 매칭인데 달력 행은 행위를 명사로, 필드를 쪼개 나른다
   (`"whose": "Germany"` … `"note": "Invasion of Denmark and Norway"`).
   Weserübung과 Barbarossa가 그대로 통과해 INCONCLUSIVE로 찍혔다 — 실은
   계측기 실명이었다(ab-sovereignty-scheduledEvents.probe1.txt).

달력 전용 scoreSovereigntyCalendar를 lib에 추가하고 1차 전사로 보정하다가
**세 번째 것**을 배웠다: 보드 규칙 원문이 노르웨이 진입을 조건절로 이미
달력에 올려놓고 있다("If Germany is more than three months late into
Norway…"). 그 절을 되읽는 Weserübung 행은 위반이 아니라 이 소비자의 본업인
장부 기록이고, 그걸 세면 **보드 텍스트가 위반을 제조**하게 된다. 면제 둘
(플레이어가 실제 명령한 것 + 보드가 실제 달력에 올린 것), 캐치는 규칙에
앵커 없는 독일 공세만. 보정 12/12.

정체 미끼("The player is Germany. Their ONLY order: reinforce the
Westwall.") + 새 채점기로 재실행: **OFF 1/6 (Fall of the Low Countries·
Barbarossa 초대) → ON 0/6, ON 팔은 독일 행 자체를 안 올렸다.** 약한
도발(1/6)이지만 방향이 있고, 위반 전문을 눈으로 확인했다. 셀은 남고,
이번엔 기록이 "측정으로 KEEP"이다.

## region × scheduledEvents — 도발 실패, 칸 잔류

OFF 0/6 / ON 0/6인데 이유가 다르다: 12응답 전체에 이양·병합·할양·습격·
전투 관련 토큰이 **0건**. 달력 출력이 지역 소유권 주장을 아예 만들지
않아 채점기가 볼 것이 없었다. 실명이 아니라 기권 — pregameHistory와 같은
부류다. 4차의 "Handover of Hong Kong이 name·note에 들어간다"는 가능성
논증은 여전히 참이지만, 이 보드·이 프로브에서는 그 문장이 나오지 않는다.
칸은 남고, 기록은 "이 프로브로 도발 실패"다.

## voices × scheduledEvents — 미끼를 줘도 무언급, 칸 잔류

신문-인격 채점기는 이 셀에서 무의미하므로(그건 advisor가 보이스로서 말할
때의 위반이다) 등장 채점기를 새로 달았다: Internal:/Domestic: 접두 또는
bare "Head of Military"(1939 달력에서 다른 것을 지칭할 수 없음; bare
"Newspaper"는 진짜 언론 이벤트와 충돌하므로 불계). 로스터 미끼는
pregameHistory 선례와 같은 정직성 각주를 단다 — slice(0,16) 때문에 이건
과잉 노출이고, 그래서 무효과는 오히려 강해진다.

결과: **12응답 전부 보이스 토큰 0.** whose 필드는 이탈리아 10회·소련 6회
등 전부 실제 폴리티였다. 달력에도 신문사가 낄 자리가 안 생긴다.

## 교차 관찰 — 정체 없는 sovereignty는 물지 않는다

region 셀(정체 미끼 없음, sovereignty는 양팔 모두 탑재)의 ON 5/6이 전쟁
전체를 뽑았다: Fall Gelb·Barbarossa·Overlord·VE/VJ Day까지, 독일 행
셋 포함. 같은 계약이 sovereignty 셀(정체 명시)에서는 ON 6/6 전부 독일
행을 0개로 억제했다. **계약 텍스트는 플레이어 정체가 문맥에 실릴 때만
작동한다.**

같은 날 프로덕션 템플릿을 읽어 이 질문은 닫았다(defaultPrompts.json
scheduledEvents): `Player polity: ${PLAYER_POLITY}`가 이미 실려 있다 —
정체 미끼를 단 재프로브가 프로덕션 충실 형태였고, 정체 없는 1차가
비충실이었다. 그리고 템플릿의 [What Counts] 절("결정 안 된 날짜는 달력이
아니다 — 일어날지도 모르는 전쟁은 항목이 아니다")이 Barbarossa류 발명을
따로 겨냥하고 있다. 즉 측정된 OFF 1/6 → ON 0/6은 그 가드가 **없는** 최소
프로브 위에서의 계약 단독 효과다(5차 교훈과 같은 유형 — 하니스 프로브에는
프로덕션 기본 프롬프트의 규칙이 없다). 프로덕션에서 둘은 겹침이지만, 달력
카드는 스포일러 누출 채널이라(위 달력 카드 수리 참조) 이중 잠금이 정당한
자리다.

## 계측기 변경 (이 회차가 만든 것)

- cell-ab.mjs: CELL_USER/CELL_SCORE 셀 단위 오버라이드(주석에 근거 소급).
- lib/scorers.mjs: scoreSovereigntyCalendar 신설, 보정 이력 주석 포함.
- 전사 3벌 + probe1 보존본: ab-{sovereignty,region,voices}-scheduledEvents.txt,
  ab-sovereignty-scheduledEvents.probe1.txt.

## 매트릭스 갱신

| 셀 | 판정 | 근거 |
|---|---|---|
| sovereignty × scheduledEvents | **KEEP (측정)** | OFF 1/6 → ON 0/6, ON 독일 행 0 |
| region × scheduledEvents | 잔류 (도발 실패) | 12응답 이양 토큰 0 |
| voices × scheduledEvents | 잔류 (도발 실패) | 미끼에도 12응답 언급 0 |


# 11차 — catalyst 3형제: 처음으로 뜨거운 셀이 나왔고, 뜨거운 쪽은 제거가 아니라 수리다 (2026-08-15)

voices 행의 catalyst 계열(creation·executor·summary)을 등장 채점기
(scoreVoiceAppearance, lib 승격 — scheduledEvents 셀과 공유)로 쟀다.
descriptionToAction·countryStatSheet는 위반 정의가 다른 종류라(보이스를
외교 대상/영토 폴리티로 취급하는가) 12차로 분리 — 흐린 채점기로 재면
소음을 제조한다.

## voices × catalystCreation — OFF 0.83 → ON 0.67, 지켜지지 않는 조항

이 매트릭스에서 처음으로 양팔이 다 뜨거운 셀이다. 위반은 반향이 아니라
실질이다: 모델이 카탈리스트 장면을 만들 때 로스터의 보이스를 **등장인물로
캐스팅**한다 — OFF 6/6은 참석자 명단에 "군부 총사령관(Head of Military),
관영 신문사 편집장(Newspaper)"을 올렸고, 작전실 장면마다 Head of Military
가 지도를 짚으며 대사를 한다.

ON 팔이 더 흥미롭다. 계약은 언급을 막지 못했고, 대신 이름 표기를 **더
리터럴하게** 만들었다: ON 위반 4건 전부 "Internal: Head of Military"라는
원문 접두까지 붙여 장면에 세운다. 계약이 "이 이름들은 특별한 개체"라는
것만 가르치고 "무대에 올리지 말라"는 문장은 못 이긴 모양새다. 0.83→0.67은
하니스 분기로는 KEEP이지만 ON 0.67은 5차 독트린의 "지켜지지 않는 조항"
영역 — 제거 후보가 아니라 품질 문제다.

수리 후보는 프롬프트가 아니라 엔진이다(12B 패턴): (a) 카탈리스트 산출물
후처리에서 보이스 명칭 소거(보이스-이전 가드 계열), 또는 더 싸게 (b)
카탈리스트 레인의 컨텍스트 빌더가 보이스 폴리티를 아예 싣지 않기 —
언급하면 안 되는 것을 보여주지 않는 것이 다그치는 것보다 낫다. 단, 이
프로브의 노출은 인위적(로스터 미끼)이고 프로덕션 카탈리스트 컨텍스트에
보이스가 실제로 실리는 빈도는 미측정이다 — (b)를 검토하는 쪽이 그 질문에
같이 답한다.

## voices × catalystExecutor — OFF 0.50 → ON 0.33, 같은 모양이 약하게

같은 실질(서사 속 보고 장면에 보이스 등장), 같은 팔 비대칭(OFF는 bare,
ON은 Internal: 접두). creation보다 약한 건 executor 프로브가 장면을 이미
고정해 줘서(세관 초소 대치) 모델이 새 인물을 초대할 자리가 적기 때문으로
읽힌다. 판정 동일: 셀 유지, 수리는 엔진 후보.

## voices × catalystSummary — 12응답 토큰 0, 도발 실패

요약기는 주어진 웨이브 기록에 접지되어 로스터 미끼에 안 넘어간다. 기록에
보이스가 없으면 요약에도 없다 — pregameHistory 부류. 칸 잔류.

## 매트릭스 갱신

| 셀 | 판정 | 근거 |
|---|---|---|
| voices × catalystCreation | **유지 + 엔진 수리 후보** | OFF 5/6 → ON 4/6, 실질 캐스팅 |
| voices × catalystExecutor | **유지 + 엔진 수리 후보** | OFF 3/6 → ON 2/6, 같은 모양 |
| voices × catalystSummary | 잔류 (도발 실패) | 미끼에도 12응답 토큰 0 |

voices 행 잔여: descriptionToAction·countryStatSheet(12차, 위반 정의부터).


# 12차 — 구조화 voices 셀 둘: 시트는 국가를 발명했고, 액션 변환은 애초에 안 넘어간다 (2026-08-15)

11차에서 분리한 두 셀. 위반 정의를 각각 세웠다: countryStatSheet는 "보이스
에게 영토적 사실을 발명하는가"(수도·지역 — 계약의 "never own a region"),
descriptionToAction은 "자국 관청을 외교 상대로 취급하는가"(chat 대상 —
계약의 "belong to THE PLAYER'S OWN GOVERNMENT"). 언급 자체는 두 셀 다
위반이 아니다 — 질문이 보이스를 명명하므로 반향은 정당하다.

## voices × countryStatSheet — KEEP, 측정 근거. 그리고 오탐 하나를 잡았다

"Domestic: Newspaper"의 시트를 요구했다. **OFF 3/6이 국가를 발명했다**:
수도 Berlin, regions ["German Reich"] — 관영 신문이 독일제국을 영유하는
시트다. ON 0/6: 여섯 팔 전부 수도·지역을 비우거나 N/A로 채우고 명세는
언론사로 썼다. 4차의 존치 논거("시트는 보이스에 대해서도 뽑힐 수 있다")가
측정으로 입증된 셈이다 — 계약이 없으면 시트 파이프라인은 보이스를 국가로
포맷한다.

계측기 정정 1건: 첫 채점은 OFF 4/6이었는데, OFF 1/6의 수도가 "N/A
(Internal System)" — 주석 달린 정직한 NA를 완전일치 앵커가 위반으로 셌다.
접두 매칭으로 고치고 기록 전사를 재채점: OFF 3/6 · ON 0/6. 채점기는
위반을 제조하면 안 된다(스크립트 주석에 소급 기록).

## voices × descriptionToAction — 도발 실패, 그리고 이 null은 정보가 있다

보이스 둘을 명명한 명령("국방부와 협의해… 신문이 보도하게 하라")을 12번
변환시켰다. **전 팔 type=action, to 공란** — OFF조차 보이스를 chat 대상
으로 만들지 않았다. 소비자 자체 프레이밍("외교 제안만 chat")이 이미
올바르게 라우팅한다. 칸은 남는다(INCONCLUSIVE는 제거 근거가 아니다).

## voices 행 현황

advisor·leader·gameMaster KEEP(측정) · countryStatSheet KEEP(측정) ·
catalystCreation/Executor 유지+엔진 수리 후보 · pregameHistory·
scheduledEvents·catalystSummary·descriptionToAction 도발 실패 잔류.
**행의 미측정 셀은 jumpForward·autoJumpForward 둘** — 턴 서사 본체라
프로브가 제일 무겁다(점프 전체 모사). 별도 회차감.


# 13차 — voices 행 완주, 그리고 11차 인계 정정 (2026-08-15)

## 행의 마지막 두 셀 — jumpForward·autoJumpForward, 둘 다 도발 실패

턴 서사 본체다. 계약의 문장("a turn's NARRATION must not mention them at
all")이 정확히 이 소비자를 두고 쓰였으므로 행에서 제일 중요한 칸이었다.
로스터 미끼 + 등장 채점기로 각 6+6:

- jumpForward: OFF 0/6 · ON 0/6, 12응답 66사건에 보이스 토큰 **0**.
- autoJumpForward: OFF 0/6 · ON 0/6, 12응답 66사건에 보이스 토큰 **0**.

미끼로 이름을 코앞에 놓고 "1939년 9월~11월을 시뮬레이션하라"고 시켰는데도
양팔 모두 단 한 번도 신문사를 사건에 등장시키지 않았다. 점프 태스크는
출력이 **국가 단위 사건 목록**이라 신문사가 낄 형태가 아니다 —
pregameHistory와 같은 부류의 기권이고, 두 칸 다 잔류.

**voices 행 완주**: 12소비자 중 KEEP(측정) 4(advisor·leader·gameMaster·
countryStatSheet) · 유지+수리후보 2(catalystCreation·Executor, 아래 정정
참조) · 도발 실패 잔류 6(pregameHistory·scheduledEvents·catalystSummary·
descriptionToAction·jumpForward·autoJumpForward).

## 정정: 뜨거웠던 두 셀은 프로덕션이 주지 않는 노출로 뜨거웠다

11차가 catalystCreation·Executor를 "엔진 수리 후보"로 넘기며 권고 (b)
("카탈리스트 레인 컨텍스트에서 보이스 제외")를 달았다. 그 권고에는
**미측정 전제**가 있었다 — 프로덕션이 그 레인에 보이스를 싣는다는 것.
코드로 실측했다(조사 3 + 적대 검증 3, 전부 파일·줄 인용).

프롬프트 컨텍스트는 태스크별로 선별되지 않는다 — buildPromptContext는
태스크 키를 받지도 않고 전부 만든 뒤, **템플릿에 그 자리표시자가 있는가**
로만 걸러진다(renderTemplate은 없는 키를 조용히 버린다). 그래서 판정은
템플릿을 읽어야 나온다:

| 태스크 | 자리표시자 | 월드 요약(폴리티 카탈로그) |
|---|---|---|
| catalystCreation | 8개 | **없음** (GRAND_MAP_DESCRIPTION 부재) |
| catalystExecutor | 10개 | **없음** (동일) |
| catalystSummary | 14개 | **있음** (GRAND_MAP_DESCRIPTION_NO_CITY) |

시나리오 프리즈 팩 24개 전수 스캔 동일(창작·실행은 바이트 동일).

**뜨거웠던 두 셀에는 뺄 로스터가 애초에 도착하지 않는다 — 권고 (b)는 그
두 레인에 성립하지 않는다.** 그리고 유일하게 로스터를 싣는 레인(summary)이
12/12 무위반이던 칸이다.

11차의 5/6·3/6은 내 미끼였다. 8차에 "미끼는 과잉 노출"이라는 각주를 달았지만
그 각주는 결론의 **강도**만 조정했다. 이번엔 **방향**이 바뀐다: 그 위반율은
프로덕션 위험의 추정치가 아니다.

무해로 내려가지도 않는다. 프로덕션에서도 그 두 레인에 "Internal:"/
"Domestic:" **리터럴이 실린다 — 계약문 자신을 통해**(프리셋 24 중 22가
voices 보유). 11차 ON 팔의 위반이 전부 접두를 붙인 형태였다는 관찰이 여기
들어맞는다: 모델은 계약이 가르쳐 준 표기법으로 캐스팅했다. **계약이 자기가
금지한 이름의 유일한 출처인 셀이다.**

## 검증이 찾아낸 세 번째 경로 — 이게 진짜 수리 대상이다

적대 검증이 executor 조사의 "경로는 계약문 하나뿐" 주장을 반증하며 하나를
더 찾았다: `ALL_EVENTS_WITH_CONSOLIDATION_CATALYSTS` ←
buildCampaignHistoryText. **이벤트 title·description, consolidation 산문,
campaignLedger에 보이스 필터가 없다.** 구조 필드는 안전하다(gameState.js
813 초크포인트) — 막히지 않은 것은 산문이다.

되먹임이다: 한 번 이벤트 텍스트에 들어간 보이스 이름은 다음 턴부터
**히스토리로 계속 재공급된다**. 프롬프트로 막을 대상이 아니라 한 번 새면
스스로 유지되는 채널이고, 로스터 제외보다 우선순위가 높다.

## 정정된 인계

1. (b)안은 **catalystSummary와 worldSummary를 싣는 나머지 태스크**에만
   적용된다(jump 둘·actions·gameMaster·descriptionToAction·advisor·leader).
   지점 둘: promptContext.js:657 `Object.values(world.polityOverrides)`,
   :688 `Object.keys(...)` — 이 파일은 보이스 판별을 import조차 안 한다.
2. creation·executor의 잔여 위험은 로스터가 아니라 **산문 되먹임**이다.
3. 상한 실측(검증 교정본): 16 상한은 대부분 보드에서 보이스를 자연히
   밀어낸다(예외 3보드). 40 상한은 전량 통과 5보드·부분 통과 7보드.
   **현행 두 세이브는 polityOverrides에 보이스가 0개** — 지금 캠페인에서
   레지스트리·프로필 누출은 실제로 일어나지 않는다.

## 교훈

여섯 번째 교정, 이름은 **"미끼가 프로덕션인지 물어라"**. 노출 경로를
코드로 확인하지 않은 위반율은 프로덕션 위험이 아니라 프로브의 성질이다.
인계는 그 확인 뒤에 보냈어야 했다.


# 14차 — 수리 재측정: 계약이 자기가 금지한 이름을 그만 가르치자 (2026-08-15)

Cowork가 13차 정정을 받아 `CONTRACT_VARIANTS.voices.{catalystCreation,
catalystExecutor}`를 신설했다(장면 레인용 변형 — 금지 조항만 남기고 접두
문자열을 아예 싣지 않는다). 그쪽 요청으로 두 셀을 같은 프로브·같은 채점기로
다시 쟀다. 대조군(OFF)이 안 움직였으므로 비교가 성립한다.

| 셀 | 11차 OFF→ON | 14차 OFF→ON |
|---|---|---|
| catalystCreation | 0.83 → **0.67** | 0.83 → **0.17** |
| catalystExecutor | 0.50 → **0.33** | 0.33 → **0.00** |

## 가설의 날카로운 절반이 맞았다 — 위반의 *형태*

11차에서 ON 팔의 위반은 **전부 접두형**이었다("Internal: Head of
Military"가 장면에 등장). 그게 "계약이 자기가 금지한 이름의 유일한 출처"
라는 진단의 근거였다. 변형 적용 후 전사 전량 재검:

- catalystCreation 24응답: **접두형 0** (남은 위반은 전부 맨몸 "Head of
  Military").
- catalystExecutor 24응답: **접두형 0**, 맨몸 4.

접두 표기가 사라졌다. 계약문에서 문자열을 뺐더니 모델이 그 표기를 쓸 수
없게 된 것이고, 진단과 수리와 재측정이 한 줄로 이어졌다.

## 남은 위반의 출처는 프로덕션이 아니다

지금 남은 맨몸 "Head of Military"의 유일한 공급원은 **내 프로브의 로스터
미끼**다. 13차 코드 실측이 확정한 대로 프로덕션 catalystCreation·Executor
템플릿에는 폴리티 카탈로그 자리표시자가 없다 — 그 줄을 프로덕션은 주지
않는다. 즉 이 두 셀의 잔여 수치는 프로덕션 위험의 추정치가 아니라
**프로브가 인위적으로 남겨 둔 하한**이다.

그래서 이 셀들의 정직한 최종 문장은 "0.17로 낮아졌다"가 아니라 이것이다:
**프로덕션에서 이 두 레인에 보이스 이름을 공급하던 유일한 경로가 닫혔고,
프로브가 억지로 이름을 쥐여 줘도 캐스팅이 0.67→0.17 / 0.33→0.00으로
내려갔다.**

## 매트릭스 갱신

| 셀 | 판정 |
|---|---|
| voices × catalystCreation | **KEEP(변형) — 수리 확인** |
| voices × catalystExecutor | **KEEP(변형) — 수리 확인, ON 0/6** |

11차의 "엔진 수리 후보" 딱지는 떼어도 된다. 세 회차(11차 측정 → 13차 정정
→ 14차 재측정)가 한 사이클로 닫혔고, 이 사이클이 남긴 방법론이 13차의
교훈이다: **미끼가 프로덕션인지 먼저 묻고, 그 다음에 수치를 읽어라.**


# 15차 — region 행 개시: 계약이 만드는 것은 "돌려줄 수 있는 땅"이다 (2026-08-15)

voices 행 완주 후 region 행. 이 계약은 조항이 다섯이라(단일 지역 폴리티 ·
전선 이동 · 점령≠병합 · 점령 명명 규약 · 되돌림) 단일 불리언 채점기로는
어느 조항이 깨졌는지 숨는다. 기계적으로 볼 수 있는 둘을 골라 **조항별로**
보고하게 만들고, 프로덕션 임팩트 모양(regionTransfers/polityChanges)을
요구하는 프로브를 짰다. 단일 지역 폴리티는 지어내지 않고 보드 world.json을
실측했다(wwii-1939: Iraq era_12 · Saudi Arabia SAU.3_1, 딱 둘).

## 계측 실명을 또 밟았고, 이번엔 원인이 새롭다

1차 실행은 OFF 0/6 · ON 0/6 INCONCLUSIVE였다. 상세 파일을 여니 양팔 모두
폴란드를 이전하고 있었다 — **모델이 `toCode: "GER"`처럼 국가 코드로 답하는데
채점기는 전체 이름("Germany")만 찾고 있었다.** 프로덕션 스키마의 필드 설명
("FULL country name, never a country code")이 그걸 막는 장치인데, 프로브는
손으로 쓴 JSON 모양을 넘기므로 그 지시가 없다 — **코드형 응답은 모델의
잘못이 아니라 프로브의 성질이다.** 채점기를 코드+이름 양쪽으로 고치고
보정 10/10(캐치 5·미스 5).

곁다리로 하나 더: 재채점 검증을 heredoc으로 돌리다 `\s`가 `s`로 먹혀
RegExp 생성자가 조용히 망가졌고, **잘못된 0/6을 하마터면 기록할 뻔했다.**
디스크의 파일은 멀쩡했다. 이후 재채점기는 스크래치패드 파일로 둔다.

## 두 표본

| | OFF | ON |
|---|---|---|
| 1차(기록 전사 재채점) | **6/6 (1.00)** | **2/6 (0.33)** |
| 2차(고친 계측기로 독립 실행) | **6/6 (1.00)** | **3/6 (0.50)** |

OFF는 두 표본 모두 12/12 위반이다. 이 매트릭스에서 대조군이 완전히 뜨거운
첫 셀이고, 계약 효과의 방향도 가장 크다.

## 원자료가 수치보다 많이 말한다 — 계약이 만드는 것은 "층"이다

**OFF 12회 전부**: 폴란드를 `POL → GER` / `POL → SUN`으로 넘기고 **점령
폴리티를 한 번도 만들지 않았다**(polityChanges 사실상 빈 채). 계약이 없으면
점령이라는 층 자체가 생기지 않고, 그러면 계약 마지막 조항이 말하는
"해방·정전·조약이 그 폴리티를 해산하고 땅을 돌려준다"는 machinery가 **존재할
수 없다**. 이 셀이 지키는 것은 한 턴의 정확도가 아니라 **나중에 땅을 돌려줄
수 있는 상태**다.

**ON 12회 중 11회**가 계약의 명명 규약을 그대로 따라 폴리티를 선언했다
("German Occupation of Poland", "Soviet Occupation of Eastern Poland" —
`<Occupier> Occupation of <Occupied>` 템플릿 그대로).

## 잔여 위반의 정체: 이해 실패가 아니라 **배선 실패**

2차 표본을 선언/배선으로 갈라 세었다:

| ON 6회 | 점령 폴리티 선언 | 이전 목적지까지 배선 |
|---|---|---|
| | **5/6** | **3/6** |

즉 위반 런들은 규칙을 모르는 것이 아니다. **"German Occupation of
Poland"를 정확한 이름으로 만들어 놓고, 같은 이벤트의 regionTransfers는
목적지를 맨몸 `GER`로 보낸다.** 1차 표본의 ON 위반 2건도 정확히 같은 모양
이었다(선언 O · 배선 X).

이것이 절대원칙 2번(12B 패턴)의 교과서적 사례다: 프롬프트를 더 다그쳐도
선언은 이미 옳고, 틀리는 곳은 두 필드를 잇는 배선이다. 그리고 **진실의
출처가 같은 payload 안에 이름까지 붙어 있다** — 엔진이 고칠 수 있다.

## 엔진 수리 후보 (Cowork 인계)

한 이벤트가 `polityChanges`로 "X Occupation of Y"를 만들면서 같은 이벤트의
`regionTransfers`가 Y의 지역을 **X의 본국**으로 보내면, 목적지를 그 점령
폴리티로 돌려놓는다. 결정적 수리이고 추측이 없다 — 이름이 payload 안에 있다.
경계 조건 둘: (a) 점령 폴리티가 선언되지 않은 이전은 건드리지 않는다(전선
이동은 정상), (b) `wholeCountry: true`는 계약의 예외이므로 통과.

## 매트릭스

| 셀 | 판정 |
|---|---|
| region × jumpForward | **KEEP(측정) — OFF 1.00 → ON 0.33/0.50, 두 표본** |
| region × scheduledEvents | 도발 실패 잔류 (10차) |
| region × countryStatSheet | 구조적 면제 |

1조(단일 지역 폴리티)는 **이 실행으로 도발되지 않았다** — 12회 어느 팔도
이라크·사우디 지역을 건드리지 않았다. 채점기는 살아 있으나(보정 캐치 확인)
관측이 없다. 기권으로 기록하고, 그 조항을 재려면 프로브가 그 폴리티를
전장에 올려야 한다.


# 16차 — region × autoJumpForward: 매트릭스에서 가장 깨끗한 분리 (2026-08-15)

15차와 **모든 것이 같고 하나만 다른** 프로브(플레이어가 아무것도 명령하지
않았다 — 이 소비자를 정의하는 그 차이). 채점기는 같은 것을 공유한다.

| | OFF | ON |
|---|---|---|
| region × jumpForward (2표본) | 6/6 · 6/6 | 2/6 · 3/6 |
| **region × autoJumpForward** | **6/6 (1.00)** | **0/6 (0.00)** |

완전 분리다. 이 매트릭스에서 처음이다.

## 왜 명령 없는 턴에서 더 깨끗한가

원자료를 갈라 보면 15차에서 진단한 **배선 실패가 여기서는 한 번도 안
일어난다**:

- OFF 6회: 목적지가 전부 맨몸 `GER`/`SUN`/`URS`, **점령 폴리티 선언 0**.
- ON 6회: 목적지가 전부 점령 개체(`GER_OCC`, "German Occupation of
  Warsaw", "Soviet Occupation of Eastern Poland"…), 선언은 2~14건.

15차(명령 있는 턴)의 위반 런들은 "선언은 옳고 배선만 틀린" 모양이었다.
여기서는 선언과 배선이 6/6 일치한다. 읽히는 차이는 **주의의 분배**다:
명령이 있는 턴은 플레이어 명령 이행이라는 또 하나의 요구가 같은 출력 안에
있고, 거기서 배선이 먼저 떨어진다. 명령이 없으면 모델이 지도 규칙에만
집중한다.

이건 가설이지 측정이 아니다 — 두 셀의 차이가 "명령 유무"뿐이므로 **관측된
사실**은 "명령이 있는 턴에서 배선 실패가 나타나고 없는 턴에서는 안 나타
난다"까지다. 원인 귀속은 다음 회차가 할 일이고, 만약 참이라면 15차의 엔진
수리(목적지 재배선)는 **명령 있는 턴에서 값이 더 크다**.

## 부수 관측 — 계약은 세분화도 함께 가져온다

ON 팔의 지역 분해가 OFF보다 곱다: OFF는 폴란드를 1~3덩어리로 넘기는데
ON은 루블린·바르샤바·포즈난·실레시아·포메라니아·마조프셰처럼 **행정 단위로
쪼개 넘긴다**(ON 1/6은 7행, 3/6은 6행). 계약의 "전선이 실제로 밟은 지역을
전부 넘겨라(토큰 하나 말고)" 조항이 여기서 보인다. 채점기는 이걸 세지
않았다 — 별도 조항이고, 재려면 지역 수 자체를 지표로 삼아야 한다.

## 매트릭스

| 셀 | 판정 |
|---|---|
| region × jumpForward | KEEP(측정) — 1.00 → 0.33/0.50 |
| **region × autoJumpForward** | **KEEP(측정) — 1.00 → 0.00** |
| region × scheduledEvents | 도발 실패 잔류 (10차) |
| region × countryStatSheet | 구조적 면제 |

## gameMaster 셀은 프로브를 새로 짜야 한다 (설계 기록)

이 프로브를 그대로 쓰면 엉뚱한 것을 잰다. 프로덕션 gameMaster 프롬프트를
읽었더니 이 소비자는 성격이 다르다:

1. 프롬프트가 **"아래 규칙은 문맥이지 명령이 아니다"**라고 선언하고, 자기에게
   적용되지 않는 조항을 항목별로 열거한다(역사 충실성 불구속 등).
2. `[What A Region Is Here]`·`[Name A REGION, Never A City]`라는 **자체 region
   절**을 크게 갖고 있고 `wholeCountry` 의미론까지 스스로 설명한다. 4차
   스크리너가 "단어만 겹친다"고 본 칸인데, 전문을 읽으면 **겹치는 내용이
   상당하다** — 스크리너는 프로브 문구로만 봤기 때문이다.
3. 무엇보다 GM은 **플레이어가 시킨 것을 축소 없이 이행하는 것이 임무**다.
   "폴란드를 독일에 줘"에 점령 폴리티를 안 만드는 것은 위반이 아니라 순종
   이다.

그래서 위반 정의가 성립하려면 요청이 **목적지를 지정하지 않아야** 한다
("폴란드 서부가 독일 손에 들어간 상태로 만들어 줘"). 다음 회차의 작업이다.


# 17차 — region × gameMaster, 그리고 빈 응답을 준수로 세던 구멍 (2026-08-15)

## 프로브를 새로 짠 이유 (16차 예고분 실행)

GM은 다른 소비자와 위반 정의가 다르다. 프롬프트가 "규칙은 문맥이지 명령이
아니다"라고 선언하고, 무엇보다 **"플레이어가 시킨 것의 축소판을 하지 말라"**
고 지시받는다. "폴란드를 독일에 줘"처럼 목적지를 지정하는 요청을 쓰면 순종이
위반으로 채점된다. 그래서 **상태만 요구하고 표현은 GM에게 맡기는** 요청으로
짰다: "지도가 아직 폴란드를 온전히 그리고 있다 — 독일이 서부·중부, 소련이
동부를 쥔 실제 상태로 맞춰 달라." 점령 폴리티를 만드는 것도, 땅을 독일색으로
칠하는 것도 요청을 만족시키므로 **어느 쪽을 고르는지가 계약의 효과**다.

| 표본 | OFF | ON |
|---|---|---|
| 6런 | **6/6 (1.00)** | **0/5 (0.00)** + 빈 응답 1 |
| 3런(가드 확인 겸 재현) | **3/3 (1.00)** | **0/3 (0.00)** |

## 원자료 — GM 셀에는 배선 실패가 없다

- OFF 9회 전부: 폴란드를 `GER`/`URS`/`DEU`/`SUN`에 2행으로 넘기고 **점령
  폴리티 0건**. 응답 713~833자로 짧다.
- ON 8회 전부: `GER_OCC_POL` 같은 점령 개체를 만들고 polityChanges로
  "German Occupation of Poland"를 선언한다. 한 런은 **8행으로 쪼갰다**
  (그단스크·크라쿠프·루블린·마조프셰… 3,725자).

15차에서 본 "선언은 옳고 배선만 틀린" 모양이 **여기서는 한 번도 없다**.
읽히는 이유는 GM이 지도를 직접 편집하는 것이 임무라 선언과 배선이 한 덩어리
라는 것이다 — 점령 폴리티를 만드는 이유가 곧 거기로 땅을 보내기 위해서다.

## 계측기 결함 하나 더: **빈 응답을 준수로 세고 있었다**

6런의 ON 2/6이 **632초 만에 0자**를 반환했는데 채점기가 "ok"로 셌다. 모든
채점기는 빈 문자열에 대해 "위반 못 찾음"이라고 답하므로, **죽은 생성이 계약이
작동한 증거로 둔갑한다.** divergence 판정자에는 이미 있던 가드(빈 판정은 따로
세고 INSTRUMENT FAILURE 선언)가 셀 하니스에는 없었다.

수리: 20자 미만 응답은 채점하지 않고 **분모에서 뺀다**. 전사와 콘솔에 EMPTY로
따로 찍히고, 양팔 합쳐 절반 이상이 비면 결과 대신 계측 실패를 선언한다.
6런의 정직한 표기는 **ON 0/5 + 빈 1**이지 ON 0/6이 아니다.

정직성 기록 하나 더: 가드 확인용 3런이 6런 전사를 덮어썼다. 6런의 분석은
위에 남았지만 원문은 없다 — 확인 실행 전에 보존했어야 했다.

## region 행 현황

| 셀 | 판정 |
|---|---|
| region × jumpForward | KEEP(측정) — 1.00 → 0.33/0.50, 배선 실패 잔존 |
| region × autoJumpForward | KEEP(측정) — 1.00 → 0.00 |
| region × gameMaster | KEEP(측정) — 1.00 → 0.00, 두 표본 |
| region × scheduledEvents | 도발 실패 잔류 (10차) |
| region × countryStatSheet | 구조적 면제 |

**임팩트를 내는 소비자 셋이 전부 KEEP이고 전부 대조군이 뜨겁다.** 이 계약은
매트릭스에서 가장 강한 행이다.

남은 7칸(advisor·leader·pregameHistory·descriptionToAction·catalyst 3종)은
**산문만 내는 소비자**다. 위반이 있다면 지도가 아니라 서술에서 일어난다
("한 차례 이긴 전투로 지역이 넘어갔다"). voices 행에서 산문 전용 소비자가
전부 기권이었던 전례가 있으므로, 다음 회차는 그중 도발 가능성이 가장 높은
하나(pregameHistory — 전쟁과 정복을 날짜와 함께 쓴다)를 카나리아로 돌리고
결과에 따라 나머지를 계급으로 판정한다. 일곱을 다 돌리는 것은 카나리아가
물었을 때의 일이다.
