# 플랜 F 심화 — OHM 역사 경계 → 시나리오 지오메트리 파이프라인

2026-08-07 설계. 목표: 시나리오 프리셋의 국경을 "현대 행정구역을 시대 소유주에
배정"(현행 Tier A)에서 **시대 실제 국경**으로 끌어올린다. 데이터는
OpenHistoricalMap(OHM)만 사용한다 — OHM 데이터는 CC0라 게임에 담아도 된다.
Omniatlas/GeaCron은 all-rights-reserved: **저작 검수용 눈으로만** 쓰고(링크),
데이터는 한 조각도 복사하지 않는다.

## 확인된 사실 (2026-08-07 프로브)

- 벡터 타일: `https://vtiles.openhistoricalmap.org/maps/ohm/{z}/{x}/{y}.pbf`
  (앱이 이미 스트리밍하는 그 서버 — z2 타일 2.8MB, 브라우저 fetch 성공).
- `land_ohm_lines` 레이어: 경계 **선** 26,642개(z2 한 장 기준), 속성
  `admin_level`, `start_date`/`end_date` + 수치형 `start_decdate`/`end_decdate`,
  `maritime`, `disputed`, `disputed_by`.
- `land_ohm_centroids` 레이어: 정치체 라벨 중심점 — `name`, `admin_level`,
  날짜쌍, `area_km2`. 폴리곤 소속 판정의 열쇠.
- 날짜 필터는 스타일이 아니라 **속성**에 있다: 앱의 maplibre-gl-dates가
  하는 일을 추출기는 속성 비교(`start_decdate <= D < end_decdate`)로 한다.

## 산출물 형식 (기존 기계와 접합)

기존 파이프라인을 그대로 탄다:

1. `scripts/extract-regions.mjs` — regions.pmtiles → `regions-seed.geojson`
   (타일 조각 스티칭 + polygon-clipping 유니온). **이 패턴의 자매 스크립트**로
   `scripts/ohm/extract-era-borders.mjs`를 만든다: 입력이 로컬 pmtiles가 아니라
   OHM 타일/오버패스이고, 날짜 D가 매개변수인 점만 다르다.
2. `scripts/presets/build-preset.mjs` + `*.spec.mjs` — 스펙이 소유권을 배정해
   시나리오 폴더를 생성. 시대 지오메트리는 시나리오 자산
   (`regions.geojson` — JSON_URLS.regionsGeojson가 이미 읽는 그 파일)으로
   들어간다.

## 추출 경로 두 가지

- **경로 1 — 타일 폴리곤화 (검증됨, 거칢)**: z2–4에서 `land_ohm_lines`를
  날짜 D로 필터 → admin_level ≤ 2 선들을 노딩·폴리곤화(turf/polygonize) →
  면(face)마다 같은 날짜의 `land_ohm_centroids` 포함 판정으로 정치체 배정.
  장점: 서버 한 곳, 전 지구 한 번에. 단점: 선→면 조립의 위상 잡음,
  타일 경계 스티칭(기존 round5 유니온 기법 재사용).
- **경로 2 — 오버패스 관계 조립 (정밀)**: OHM 오버패스에서
  `relation[type=boundary][admin_level=2]` + ISO 날짜 문자열 비교
  (`t["start_date"] <= "1939-09-01"` — ISO는 사전순=시간순)로 시대 관계를
  받아 멤버 웨이로 링을 조립. 장점: 정치체별 정확한 멀티폴리곤.
  단점: 요청 수·속도 제한, 조립 코드.

권장: **경로 1로 전 지구 골격 → 경로 2로 주요 분쟁 경계만 정밀 교체**.
검수는 Omniatlas/GeaCron 화면과 눈 대조(링크만 기록).

## 단계

- F-1: `scripts/ohm/extract-era-borders.mjs` 골격 — 날짜 D, bbox, zoom을 받아
  필터된 경계선+중심점 GeoJSON 덤프 (조립 전 원자재 검사용).
- F-2: 폴리곤화 + 정치체 배정 + 스티칭 → `era-borders-<date>.geojson`.
- F-3: 프리셋 접합 — 스펙에 `eraGeometry: "1939-09-01"` 필드를 주면
  build-preset가 시나리오 `regions.geojson`을 시대판으로 교체. 게임 쪽은
  변경 없음(이미 시나리오별 지오메트리를 읽는다).
- F-4: WWII++ 1939로 1차 실증 (동프로이센, 전간기 폴란드 동부 국경이
  현대 배정판과 눈에 띄게 다른 지점 — 검수 앵커).
- F-5 (선택): admin_level 4 시대 하위구역 → 지역 단위 플레이 정밀화.

## F-1 실측 (2026-08-09 — 1939-09-01 · z2 세계 · 사용자 PC 실제 덤프)

플레이어가 로컬에서 첫 덤프를 떴고(선 1.4MB·중심점 47KB), Cowork가 원본을
받아 측정했다. F-2 설계를 바꾸는 사실들:

- 선 5,552개(정점 21,309) / 중심점 180개. 추출기 로직 재검증 불일치 0.
- 구조 건전: 섬 폐곡선 376, 삼합점(차수3) 125. 타일 이음매는 대부분 양쪽
  타일이 같은 반올림 점으로 끝나 자기결합(차수2) — 이음매 근처 외톨이는
  42개뿐(반올림 흔들림 ~21쌍). → F-2는 ε-스냅으로 봉합.
- **admin_level 1 = 제국 계층**(대영제국·프랑스 식민제국 등 중심점 6, 선
  1,220)이 국가(2) 경계 위에 좌표까지 겹쳐 감 — 차수4 노드 973개의 정체.
  → **면(face) 조립은 admin_level 2 선만 사용**, 1은 참조용으로만 보존.
- **z2 중심점은 타일러가 면적순으로 솎는다**: 폴란드·독일·일본 라벨이 없다
  (경계선 자체는 있음). area_km2는 해역·극지 클레임 포함(호주 1.22억km²) —
  라벨 순위용 수치일 뿐. → **중심점 수집은 z4+에서** (덤프가 작아 비용 무시).
- 진짜 끊김(이음매와 무관한 차수1) 519개 — z2 단순화로 짧은 웨이 탈락 +
  OHM 미완 구간(멕시코-미국 국경 표본 다수). → F-2: 선은 z3–4로 뜨고,
  스냅 후에도 못 닫힌 면은 중심점 0개/2개+ 면과 함께 **국가별 현대 배정
  (Tier A) 폴백 + 사유 로그** — 조용히 틀리지 않는다.
- 같은 영토의 중복 버전 실존(뉴펀들랜드: 자치령 1927→1949와 위원회정부
  1934→1949가 동시 유효 — OHM 데이터 결함). → **latest-start 우선 + 충돌
  로그** 규칙.
- 창 규칙 실증: 소련 1926→**1939-10-06**(폴란드 전역 종료일 판갈이)이 9/1에
  정확히 살아 있고, 만주국(1932→1945-08-17)·티베트(1912→1951)·中國
  (1935→1945)·영제국(1939-04-06→1940-06-14)이 등장. 시대 슬라이스가 진짜다.
- 다음 검증 러닝(로컬, 타일 4장): `node scripts/ohm/extract-era-borders.mjs
  1939-09-01 --zoom 4 --bbox 10,45,35,60` — 폴란드·독일 중심점 등장 여부와
  동프로이센 선 디테일 확인 → F-2 줌 파라미터 확정.

### z4 검증 러닝 실측 (2026-08-09 — 동유럽 창, 타일 4장, 사용자 PC)

- **판갈이 품질은 정밀하다**: 독일-폴란드 계열 169선이 정확히 1920-01-10
  (베르사유 발효)→1939-10-26(병합·총독부), 슬로바키아 1939-03-14→1945-05-08,
  스페인 1939-04-01(내전 종료)→1956, 에스토니아 1918-02-24→1940-08-06(소련
  병합), 이탈리아 1882→1943-09-08(휴전). 9/1엔 침공 전 폴란드가 온전히 선다
  — 병합 판갈이가 10월이라, 개전일 시나리오의 원판으로 정확.
- 회랑/동프로이센 앵커 박스(16-24E 52-56N): z4 199선 699정점 = z2의 3배.
  F-2 조립 캘리브레이션엔 충분, 최종 시나리오 품질은 z5로 재평가.
- **이음매 완전 자기결합**: z4 내부 이음매(22.5E·55.78N) 미결합 0. 반올림
  계약 실증 — ε-스냅은 보험.
- **타일 중심점은 이름 원천이 될 수 없다(결론)**: z4 4타일에 3개뿐(Norge·
  Latvija·България — 관계가 유효 폴리곤인 나라들만). 독일·폴란드·프랑스
  부재는 관계 지오메트리 미완으로 타일러가 중심점을 못 만든 것(z2 "면적
  솎임" 가설 수정, 창 규칙 무죄). → **이름은 OHM 오버패스**: admin_level=2
  관계 날짜 필터 + `out tags center`(태그·중심만) — 깨진 관계도 center는
  나온다. 중심이 영토 밖에 떨어질 수 있는 기형 다각형은 배정 로그로 방어.
- **끊김의 정체와 자연 상수**: 창 내 진짜 끊김 208개 → **0.06° 스냅으로
  174개 결합, 이후 평탄역**(0.15°에서 +2). ~~잔여 34개는 maritime 필터 탓~~
  → **z4m 재검증으로 기각** (아래 참조).

### z4m 재검증 — maritime 가설 기각 기록 (2026-08-09, 사용자 PC 실측)

- **maritime는 아무것도 안 닫는다**: admin_2 차수-1 끊김이 maritime 제외
  226 → 포함 225, 0.06° 스냅 후 44 → 45 (±1). 해안을 따라가는 경계는
  평범한 admin_2 선이고(단치히만 현장 검증: 만 연안 전 구간 maritime 태그
  없음, 1920-01-10→1939-10-26 베르사유 판), maritime=yes는 먼바다 경계뿐
  — 오히려 제 끊김을 새로 들여온다. → **F-2는 F-1 기본값 그대로 maritime
  제외.**
- 단치히만 "미결"은 지표 오류였다(홀수 차수 지표에 정상 삼합점 혼입).
  실제로는 0.022° 갭 — 스냅 범위 안.
- **버퍼 오버슛 발견**: 끊김 일부가 정확히 타일 경계 + 1.40625°(타일폭/16,
  MVT 버퍼 256/4096)에 정렬 — x=23.90625 삼중 표본(보트니아만). 타일러가
  일부 지오메트리를 버퍼째 내보낸다. → F-2 디코드에 **타일 정확 경계 클립**
  단계 추가: 버퍼 중복 제거 + 이음매 결합 보장.
- **F-2 파이프라인 확정(수정판)**: ① admin_2, maritime 제외 → ② 타일 정확
  경계 클립 → ③ round5 병합 + 0.06° 스냅 → ④ 면 조립 → ⑤ 오버패스 이름
  배정 → ⑥ 현대 육지 마스크(regions-seed 유니온) 클립 — 바다 면 제거·해안
  정밀도·해안 누수 면 복원 겸용 → ⑦ 미배정/다중 배정 면은 국가별 현대
  배정(Tier A) 폴백 + 사유 로그. admin_1(제국 계층)은 조립 제외.

### F-2 구현 완료 (2026-08-09) — 구현이 설계를 두 곳 교정

- **해안은 후처리 클립이 아니라 그래프 융합**: 구현 중 실측으로 OHM 선
  타일에 해안선이 사실상 없음이 판명(리가만 정점 10개 — 전부 육상 국경;
  유효 관계인 라트비아도 바다 쪽은 못 닫음). ⑥은 "클립"에서 **현대 해안을
  조립 그래프에 주입하고 시대 국경 끝점을 해안 세그먼트에 스냅(분할 접합)**
  으로 승격 — regions-seed가 F-2의 필수 입력이 됐다(국가별 유니온→트리
  병합, 실패는 조각 분리 유지+카운트).
- **창 덤프엔 프레임 규칙**: 창 가장자리에서 잘린 국경은 창 테두리 세그먼트
  (접점에서 분할)에 접합 — 없으면 가지치기 연쇄가 유럽 창 세그먼트의 55%를
  삼켰다(실측). 세계 덤프도 동일 경로(±180 경계 나라는 분할 — 기지 한계).
- 검증(커밋된 실데이터 픽스처): **라트비아 9.633 deg² 단일 면**(실면적
  일치), 폴란드 면 51 deg² 존재(무이름 — 오버패스 이름 대기), 발트 창 이상
  보행 0·바이트 결정론. 스위트 tests/ohm-assemble.mjs 16 + ohm.mjs 22.
- 자산: lib/assembleFaces.mjs(순수 조립기), fetch-era-polities.mjs(오버패스
  이름, PC 전용), assemble-era-borders.mjs(조립 CLI, 어디서든), 추출기에
  타일 정확경계 클립 + 파일명 -z줌. isoToDecdateEnd(연 단위 종료일 = 기간
  끝 — 中國 "1945" 실측 소급 일치).
- 남은 캘리브레이션: 실전 유럽/세계 런(신형 덤프 + 오버패스 이름) 후 F-3.
- 예상 잔여: 유럽 창 스냅 후 ~44 (노르웨이 피오르·영국 해안 등 z4 단순화
  탈락) — 버퍼 클립이 일부 더 닫고, 나머지는 ⑥·⑦이 흡수. z5 승격 시 재측정.

## 전세계 커버리지 실측과 출처 사다리 (2026-08-09, 플레이어 관측 + 실측)

플레이어가 OHM 사이트에서 아시아 부실을 관측 → 전 지구 재점검(1939 z2,
지역별 정점수/유효 정치체수). 결과, **1939 유효 폴리곤 지도는 사실상
식민제국 지도다**: 아프리카 51개(!), 중동 16, 동남아 15(말레이 보호령
4개까지), 미주 39, 오세아니아 ~21 — 식민 경계는 길고 곧아 매핑이 잘 돼
있다. 반면 **유럽 대륙 주요국 전멸**(독일·프랑스·이탈리아 본토·스페인·
폴란드·베네룩스·덴마크·핀란드·발트 2/3·체코·헝가리·루마니아·유고·스위스
전부 무효 — 유효는 UK·포르투갈·스웨덴·노르웨이·아이슬란드·그리스·불가리아·
라트비아·터키+도서들), **동아시아 6개**(만주국·中國·티베트급 3 + 조계 3;
일본 본토·일본령 조선·몽골·멍장 부재), 소련권은 소련+투바인민공화국(Tьʙа).

단, 유럽은 선(라인)이 조밀(정점 3,924, 베르사유 판갈이 정밀)해서 **면 조립
(F-2)으로 커버된다** — 유효 관계 폴리곤이 필요 없는 게 F-2 설계의 요점.
동아시아는 선도 성겨서(정점 1,190) 사다리 2단이 주력이 된다.

지역별 주력 단(rung): 유럽 1단(선 조립) · 아프리카/중동/남아시아/동남아/
미주/오세아니아 1단(선 조밀+유효 관계 다수) · 동아시아 2단(+만주국·티베트·
조계는 1단) · 소련 1단 · 1444 이전 프리셋 3단.

처방은 나라(polity)별 **출처 사다리** — 조립기가 출처를 전부 로그한다:

1. **OHM (CC0, 정밀)** — 면이 닫히고 이름이 붙는 곳. 아시아에서도 만주국·
   티베트·인도차이나·조계들은 진짜 시대 국경.
2. **현대 조합 (기존 Tier A)** — 식민 경계가 현대 국경이 된 곳은 현대 지역
   조합이 곧 시대 국경이다: 일본제국 = 현대 일본+한반도+대만(압록강·두만강
   동일), 몽골 = 현대 몽골. 예외: 가라후토(사할린 50N 분할)는 스펙 별도 처리.
3. **historical-basemaps (aourednik, GPL-3.0)** — 조악·전지구·전시대
   (world_1938/1800/1300/bc1000 등, -123000년부터). 1·2가 다 안 되는 곳
   (멍장 내측 등)과 **1444 이전 깊은 과거 프리셋**(colonial-1650, mongol-1300,
   roman-117, bronze-1200bc)의 골격. 대륙 스케일 정밀도임을 명시하고 쓴다.
   저장소: github.com/aourednik/historical-basemaps (레포가 정확도 주의를
   명시 — 학술 아님, 게임 골격엔 적합).

기대치 관리: 중일전쟁 전선(관내 점령지)은 어느 데이터셋에도 국경으로 없다 —
만주국까지가 국경, 전선은 게임 오버레이/서사의 몫. Omniatlas/GeaCron 금지는
변함없음(눈 검수·링크만).

**프로빈스(하위구역) 방침** (플레이어 질문 2026-08-09): 원본 팍스
히스토리아에서 프로빈스 데이터를 직접 추출하는 것은 **금지** —
Omniatlas/GeaCron과 동급(눈 참고·비교 검수만, 데이터 복사 불가). 대신
게임엔 이미 전 지구 프로빈스 층이 있다: regions.pmtiles(GADM admin-1) —
프리셋 기계가 지금도 이 지역들을 시대 소유주에 배정한다("각 프리셋에 맞는
프로빈스"는 이미 작동 중). 플랜 F는 그 위 국가 외곽선을 시대판으로 올리는
것이고, F-5는 OHM admin_level 4가 실제 매핑된 곳(추정: 유럽 일부 — 예:
프로이센 주들)만 시대 하위구역으로 선택 승격한다. 측정은 추출기 플래그
`--max-admin-level 4`로 즉시 가능(테스트에 핀 있음).

## F-2.5 수리 실측 (2026-08-09 — 클라우드 재조립 반복)

- **교차 노딩 도입**(그리드 해시, 고정점 반복 ≤5패스): 특히 **그림자 간선**
  — 정점을 공유하는 동방향 공선 이중선(펜반 철도 회랑!)이 fan 각도 퇴화 →
  보행이 시작점 없는 순환에 빠지는 트랩의 진범. 분할하면 중복 구간이 Set
  인접에서 자동 붕괴. 이상 보행 321→28, 창 내 라벨 유실 22→**1**.
- **해안 유니온 폐기 → 패리티 디졸브**: polygon-clipping 대륙 병합의
  비결정 실패 → 중복 경계 잔존 → 링별 데시메이션 어긋남 → 준평행 교차
  폭발(4,676 실측). regions-seed의 설계 보장(이웃 지역 정점 일치) 위에서
  세그먼트 홀짝 판정(짝수=내부 국경 제거, 홀수=해안·호안)으로 정확·선형·
  무실패. 특이 홀짝 0 실측, 50초 → 10초.
- **열린 해안 사슬은 면적 필터 면제**: 가늘고 긴 열린 해안 조각의 암묵
  폐합 면적 ≈ 0 → "작은 섬"으로 오인 삭제 → 본토 고리 전체가 가지치기로
  풀림(4,759 엣지 실측). 닫힌 사슬만 면적 필터. coastMinRingArea 완화
  (1e-5)는 미세섬 폭발로 역효과 실측(면 22,702, 폴란드 유실) — 3e-4 유지.
- 배정 수리: lostLabels 보고(어느 면에도 없는 라벨 = 진단 목록), 중심
  오버라이드(scripts/ohm/data/center-overrides.json — 프랑스는 알제리 포함
  bbox라 중심이 지중해 남쪽, 영국은 북해협: 손배치 내부점), Граница류
  경계선 관계 필터.
- 결과(1939-09-01 유럽 z4): 자동 배정 **26 → 35개국**(+벨기에·헝가리·
  에스토니아·영국·리투아니아·리비아·레바논·시리아 등), 창 내 라벨 유실
  1(알바니아 보호령). 발트 픽스처 재고정: 라트비아 9.387 deg², 이상 0.

## 클로드 코드 리뷰 브리핑 (F 마무리 전 — 플레이어 지시 2026-08-09)

플레이어: "F계획 마무리 직전에 클로드 코드한테 한번 훑어보게 한 후에 미비한
점이나 개선할 부분 있으면 작업할 수 있게끔 미리 준비." 검토 범위와 열린
문제를 여기 고정한다.

**검토 대상**: scripts/ohm/** 전체 — 특히 lib/assembleFaces.mjs(평면 보행
회전 규칙·노딩·스냅 순서·구멍 부착), lib/eraBorders.mjs(날짜·타일 수학),
CLI 3종 + render-era-preview.mjs. tests/ohm*.mjs의 픽스처 계약(발트 수치
재고정 시 사유를 커밋 메시지에).

**재현 경로**(전부 사용자 PC 가능, ①만 네트워크):
① `node scripts/ohm/fetch-era-polities.mjs 1939-09-01`
② `node scripts/ohm/extract-era-borders.mjs 1939-09-01 --zoom 4 --bbox -10,35,45,71`
③ `node scripts/ohm/assemble-era-borders.mjs scripts/ohm/out/era-lines-1939-09-01-z4.geojson --polities scripts/ohm/out/era-polities-1939-09-01.json`
④ `node scripts/ohm/render-era-preview.mjs <borders> <lines>` — 눈 검수.

**열린 문제(우선순위순)**:
1. **지중해 메가면**: 프랑스·이탈리아·노르웨이·터키·스웨덴·핀란드 중심이
   바다와 한 면 — 해안 연속성 국소 결함. 단서: 리옹만 43.5N 부근 링 왕복
   흔적(양쪽이 같은 면인 비분리 해안 = 그 서쪽 어딘가 개방), 해안 단독
   조립은 분리 성공 → 시대 선 융합 후 실패하므로 상호작용 결함. 후보 접근:
   열린 사슬 끝 해안-해안 우선 용접, 구멍 위치 국소 이분 탐색.
2. **독일 4-라벨 충돌면**: Deutsches Reich+Lëtzebuerg+Slovenská(런에 따라
   +Liechtenstein) — 상호 국경 갭으로 한 면에 병합. DE-LU·DE-SK(1938 빈
   중재 신국경)·LI 링의 갭 좌표 측정 후 스냅 조정 또는 OHM 데이터 보고.
3. 노딩 비수렴 잔여 ~6건/패스(분할점이 기존 끝점으로 반올림되며 무한
   재검출 의심 — T_EPS 근방 처리 정밀화).
4. 바티칸·모나코 z4 스텁 → 이탈리아+바티칸+모나코 충돌: 도시국가는 z4
   축척 밖. 정책 제안: area_km2 < 임계 정치체는 배정 후보에서 제외하고
   메모만(도려내기보다 나음) — 결정 필요.
5. 성능: 유럽 창 ~30초(노딩 다패스 지배) — 세계 런 외삽·그리드 셀 튜닝.
6. 대비: ±180 자오선 분할(세계 런), 캐스피해 같은 내해 처리 확인.

**불변 원칙**: Omniatlas/GeaCron 눈 검수만(데이터 복사 금지) · 자산은
OHM(CC0)+저장소 계보(GADM 유래)만 · 모든 드롭은 카운트+사유 · 상수는 측정
근거 필수(0.06° 스냅, 3e-4 섬 하한처럼).

## 실행 환경 메모

추출은 Node 스크립트(오프라인, 사용자 PC 또는 개발 클론)로 돈다.
클라우드 세션에서 돌릴 땐 타일 fetch가 필요하므로, 브라우저 컨텍스트
(앱이 이미 쓰는 타일 서버) 경유로 원자재를 받는 경로가 검증돼 있다.
의존성은 저장소에 이미 있다: pmtiles, @mapbox/vector-tile, pbf,
polygon-clipping (+ 폴리곤화에 turf 계열 추가 필요).
